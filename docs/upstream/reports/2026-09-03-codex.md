# Upstream scan report - codex (OpenAI Codex CLI)

- Date: 2026-09-03
- Executable: `codex`
- Package: `@openai/codex`
- Release channel: npm
- Repo: https://github.com/openai/codex
- Install docs: https://developers.openai.com/codex/cli

## Mechanical source of truth

Flags, output modes, session/resume rules, permission modes, and env contracts are defined and enforced ONLY by `src/upstream-contracts.ts` (`UPSTREAM_CLI_CONTRACTS.codex`). This report is advisory; it never changes validation behaviour.

- Contract flags tracked: **28**
- Subcommands tracked: **26** top-level
- Conformance fixtures: **14**
- Watched categories: `flags`, `sandbox-modes`, `approval-modes`, `session-resume`, `output-schema`

## Tracked sources

- https://api.github.com/repos/openai/codex/releases/latest _(not fetched; run with `--live`)_
- https://learn.chatgpt.com/docs/changelog/rss.xml _(not fetched; run with `--live`)_

## Installed CLI probe

- Contract target version: `codex-cli 0.152.1`
- Installed version: `codex-cli 0.152.1`
- Version comparison: **matches**
- Root commands discovered: **27**
- Root catalog drift: none
- Root request flag-shape drift: 0 arity, 0 enum finding(s)

## Declared subcommand help surfaces

- Probed declared command paths: **28**
- Drifted command paths: **0**
- Risk counts: writes_local_config=8, starts_server=3, destructive=2, network=2, read_only=3, executes_agent=7, auth=2, updates_binary=1


## Findings

- No findings. (Default offline scan is advisory-only.)

## Next actions

1. If a watched category changed upstream, update the relevant flag/enum in `src/upstream-contracts.ts` and add/adjust a conformance fixture.
2. Mirror any `sourceUrls` / `watchCategories` change into `provider-sources.dag.toml`.
3. Re-run `npm run upstream:contracts` to confirm fixtures + TOML-sync pass.
