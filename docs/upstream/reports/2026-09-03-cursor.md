# Upstream scan report - cursor (Cursor Agent CLI)

- Date: 2026-09-03
- Executable: `cursor-agent`
- Package: `cursor-agent`
- Release channel: vendor
- Repo: not declared
- Install docs: https://cursor.com/cli

## Mechanical source of truth

Flags, output modes, session/resume rules, permission modes, and env contracts are defined and enforced ONLY by `src/upstream-contracts.ts` (`UPSTREAM_CLI_CONTRACTS.cursor`). This report is advisory; it never changes validation behaviour.

- Contract flags tracked: **12**
- Subcommands tracked: **18** top-level
- Conformance fixtures: **9**
- Watched categories: `flags`, `subcommands`, `session-resume`, `execution-modes`, `acp-entrypoint`

## Tracked sources

- https://cursor.com/cli _(not fetched; run with `--live`)_
- https://cursor.com/docs/cli/acp _(not fetched; run with `--live`)_
- https://cursor.com/docs/cli/reference/parameters _(not fetched; run with `--live`)_

## Installed CLI probe

- Contract target version: `cursor-agent 2026.09.02-c22c1a3`
- Installed version: `2026.09.02-c22c1a3`
- Version comparison: **matches**
- Root commands discovered: **21**
- Root catalog drift: none
- Root request flag-shape drift: 0 arity, 0 enum finding(s)

## Declared subcommand help surfaces

- Probed declared command paths: **18**
- Drifted command paths: **0**
- Risk counts: read_only=3, executes_agent=3, writes_local_config=6, network=2, auth=2, updates_binary=1, starts_server=1


## Findings

- No findings. (Default offline scan is advisory-only.)

## Next actions

1. If a watched category changed upstream, update the relevant flag/enum in `src/upstream-contracts.ts` and add/adjust a conformance fixture.
2. Mirror any `sourceUrls` / `watchCategories` change into `provider-sources.dag.toml`.
3. Re-run `npm run upstream:contracts` to confirm fixtures + TOML-sync pass.
