# Upstream scan report - claude (Claude Code CLI)

- Date: 2026-09-03
- Executable: `claude`
- Package: `@anthropic-ai/claude-code`
- Release channel: npm
- Repo: not declared
- Install docs: https://code.claude.com/docs/en/overview

## Mechanical source of truth

Flags, output modes, session/resume rules, permission modes, and env contracts are defined and enforced ONLY by `src/upstream-contracts.ts` (`UPSTREAM_CLI_CONTRACTS.claude`). This report is advisory; it never changes validation behaviour.

- Contract flags tracked: **42**
- Subcommands tracked: **21** top-level
- Conformance fixtures: **14**
- Watched categories: `flags`, `output-formats`, `permission-modes`, `session-resume`, `models`

## Tracked sources

- https://code.claude.com/docs/en/changelog.md _(not fetched; run with `--live`)_

## Installed CLI probe

- Contract target version: `claude 2.1.259`
- Installed version: `2.1.259 (Claude Code)`
- Version comparison: **matches**
- Root commands discovered: **21**
- Root catalog drift: none
- Root request flag-shape drift: 0 arity, 0 enum finding(s)

## Declared subcommand help surfaces

- Probed declared command paths: **21**
- Drifted command paths: **0**
- Risk counts: writes_local_config=14, auth=2, read_only=1, starts_server=1, executes_agent=1, updates_binary=2


## Findings

- No findings. (Default offline scan is advisory-only.)

## Next actions

1. If a watched category changed upstream, update the relevant flag/enum in `src/upstream-contracts.ts` and add/adjust a conformance fixture.
2. Mirror any `sourceUrls` / `watchCategories` change into `provider-sources.dag.toml`.
3. Re-run `npm run upstream:contracts` to confirm fixtures + TOML-sync pass.
