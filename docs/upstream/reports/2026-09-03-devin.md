# Upstream scan report - devin (Cognition Devin CLI)

- Date: 2026-09-03
- Executable: `devin`
- Package: `devin`
- Release channel: vendor
- Repo: not declared
- Install docs: https://docs.devin.ai/cli

## Mechanical source of truth

Flags, output modes, session/resume rules, permission modes, and env contracts are defined and enforced ONLY by `src/upstream-contracts.ts` (`UPSTREAM_CLI_CONTRACTS.devin`). This report is advisory; it never changes validation behaviour.

- Contract flags tracked: **11**
- Subcommands tracked: **19** top-level
- Conformance fixtures: **17**
- Watched categories: `flags`, `subcommands`, `permission-modes`, `acp-entrypoint`

## Tracked sources

- https://cli.devin.ai/docs/reference/commands _(not fetched; run with `--live`)_
- https://docs.devin.ai/cli _(not fetched; run with `--live`)_
- https://docs.devin.ai/cli/changelog/stable _(not fetched; run with `--live`)_

## Installed CLI probe

- Contract target version: `devin 3000.6.12 (fc947ca5)`
- Installed version: `devin 3000.6.12 (fc947ca5)`
- Version comparison: **matches**
- Root commands discovered: **20**
- Root catalog drift: none
- Root request flag-shape drift: 0 arity, 0 enum finding(s)

## Declared subcommand help surfaces

- Probed declared command paths: **19**
- Drifted command paths: **0**
- Risk counts: starts_server=1, auth=1, network=1, writes_local_config=11, read_only=3, destructive=1, updates_binary=1


## Findings

- **warning**: Installed devin binary no longer advertises declared contract flag(s): --agent-config.

## Next actions

1. If a watched category changed upstream, update the relevant flag/enum in `src/upstream-contracts.ts` and add/adjust a conformance fixture.
2. Mirror any `sourceUrls` / `watchCategories` change into `provider-sources.dag.toml`.
3. Re-run `npm run upstream:contracts` to confirm fixtures + TOML-sync pass.
