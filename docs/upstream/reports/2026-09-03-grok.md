# Upstream scan report - grok (xAI Grok CLI)

- Date: 2026-09-03
- Executable: `grok`
- Package: not declared
- Release channel: vendor
- Repo: not declared
- Install docs: https://docs.x.ai/build/overview

## Mechanical source of truth

Flags, output modes, session/resume rules, permission modes, and env contracts are defined and enforced ONLY by `src/upstream-contracts.ts` (`UPSTREAM_CLI_CONTRACTS.grok`). This report is advisory; it never changes validation behaviour.

- Contract flags tracked: **43**
- Subcommands tracked: **22** top-level
- Conformance fixtures: **27**
- Watched categories: `flags`, `permission-modes`, `session-resume`, `sandbox`, `output-formats`

## Tracked sources

- https://docs.x.ai/developers/release-notes.md _(not fetched; run with `--live`)_

## Installed CLI probe

- Contract target version: `grok 1.0.13 (5e9a58528b76)`
- Installed version: `grok 1.0.13 (5e9a58528b76)`
- Version comparison: **matches**
- Root commands discovered: **23**
- Root catalog drift: none
- Root request flag-shape drift: 0 arity, 0 enum finding(s)

## Declared subcommand help surfaces

- Probed declared command paths: **26**
- Drifted command paths: **0**
- Risk counts: executes_agent=2, starts_server=4, writes_local_config=7, read_only=8, auth=2, network=1, updates_binary=1, destructive=1


## Findings

- **warning**: Installed grok binary no longer advertises declared contract flag(s): --best-of-n, --check, --experimental-memory, --no-memory.

## Next actions

1. If a watched category changed upstream, update the relevant flag/enum in `src/upstream-contracts.ts` and add/adjust a conformance fixture.
2. Mirror any `sourceUrls` / `watchCategories` change into `provider-sources.dag.toml`.
3. Re-run `npm run upstream:contracts` to confirm fixtures + TOML-sync pass.
