**Verification summary (against the three provided files + source tree at the worktree, which matches master@dec94cc for src/):**

**Own round-1 items (Grok):**
- R1-9 (mirror equality): Resolved. Spec 5.1 now explicitly describes the post-`buildCliResponse` worktree banner sites (e.g. `formatWorktreePrefix` at index.ts:5403-5406, 5870-5872, 7141-7143, 8205-8208, 8690-8692), states that banners are gateway-owned and deliberately unmirrored (cross-ref to comment at :4054-4055), and qualifies the invariant: "the two surfaces are identical up to that documented, gateway-owned banner prefix, and the banner is never part of the compressor's input or output." Matrix item 8 now includes the worktree-case assertion with the "equal modulo that documented prefix" qualifier. Source confirms the banner is applied only to `content[0].text` after the shared `finalStdout` assignment to both mirrors.
- R1-2 (Claude stream-json async NDJSON): Resolved. Spec 5.2 now documents the current behavior (only Codex does `result.stdout` swap today; Claude stream-json leaves raw NDJSON in `result.stdout` while prose is in sibling `parsed.text` at llm_job_result ~11569-11613), then requires the Codex-precedent swap under the compression flag (`result.stdout := parsed.text`), followed by `compressDisplayText()` on the display text. The `parsed` sibling keeps its usage/cost/model/numTurns fields. Matrix item 9 explicitly covers the Claude async case (compressed prose in `result.stdout`, raw NDJSON untouched in store/FR, usage fields survive; off = byte-identical to today). Source confirms the envelope construction and current Claude vs. Codex divergence.
- R1-1 (blank-line/ANSI etc. vs. "lossless"): Resolved. All absolute "lossless" language removed. C3 now defines the three named tiers (B: byte-preserving content tokens, e.g. JSON lexer + envelope compaction; F: counted folds with sentinels, reconstructible multiset/frame count; P: presentation-discarding, e.g. ANSI/CR/trailing ws/3+-blank collapse, *not* reconstructible from output alone). Explicitly calls out that Tier P bytes (including blank-line runs) require the 5.3 flight-recorder read-back. Tests in matrix 5/8/10 assert per-tier guarantees in their own terms (content tokens, sentinel roundtrip, content-only + read-back). All prior "lossless" claims rewritten.
- R1-18 (wiring inventory): Resolved. Section 5.5 now titled "Module layout and the full wiring surface" and replaces the prior narrow claim with an explicit inventory (buildCliResponse + ExtendedToolResponse field; llm_job_result display swap + compress + envelope; effective decision at enqueue; JobStore column + interface; async dedup key; flight recorder columns + recordCompressionTelemetry + async parity fix; config; request schemas). Call sites remain the two primary ones, but surface is honestly enumerated.

**Spot-checks of other round-1 dispositions (against revised spec text + source citations):**
- R1-3/4 (outputSchema bypass + dedup key): 5.2 correctly states effective decision (request.compressResponse ?? config + !json + no schema) is computed at enqueue (where full params including Codex outputSchema are available), persisted as additive `compress_response` (NULL = not requested), and honored on read-back. Dedup folds the effective decision into `buildRequestKey` following the exact `|fmt:` precedent (async-job-manager.ts:896-914, only-codex, absent/defaults share key, explicit-on splits). Matrix 9 asserts both collision directions. Job schema (job-store.ts:335) and buildRequestKey logic match.
- R1-5 (telemetry path): C5 + section 8 correctly route compression facts via new additive `recordCompressionTelemetry` (write-once on the compression_* columns only, after `safeFlightComplete`), not `logComplete`. Threads `CompressResult` from buildCliResponse. Covers the two late cases (Claude stream-json: FR at ~8174 before buildCliResponse at 8192; async at llm_job_result read time after writeFlightComplete). Async `optimizationApplied: false` hardcodes (async-job-manager.ts:700/711/1309 area) are called out for the parity fix.
- R1-6/7/8 (C7/5.3/C1 precision): C1 and C7 restated as byte-identity invariant (compressor never mutates bytes seen by parsers/extractors/FR). Explicit Claude stream-json nuance documented (buildCliResponse receives `parsed.text`; extractUsageAndCost at index.ts:4065 still receives the `stdout` arg to build, not finalStdout; async extracts from raw `job.stdout`). Full per-provider FR list in 5.3 (gemini/grok/devin/cursor/mistral raw stdout; Claude stream-json `parsed.text` pre-build; other Claude raw; codex `codexFrResponse`; async raw + codexFr only for process codex) exactly matches source call sites (e.g. index.ts:5412,5878,6277,6730,7149,8177,8215,8663; async-job-manager.ts:1292 area). `codexFrResponse` and "logs raw job.stdout" imprecision fixed. PostgresJobStore stub reality stated (job-store.ts:944-955 throws in ctor; only SQLite + Memory real; interface types updated).
- R1-10/11/13/14/15/16/17/19: 5.1 diagram now uses explicit reassignments. 5.3 has exhaustive FR enumeration. 6.2 makes dangerous-sequence (cursor-movement/alt-screen) part of router classification (identity direct, defense-in-depth in transform). Matrix 7 + 6.4 cover fenced sentinel-like bodies (no escaping inside; unclosed-fence safety). C7 scope note records API deviation. 6.4 + matrix 5 extend leading note to lit-only. Section 10 adds PR-3 offset desync note. Section 8 cites annex Part 1 section 5 for divisor table.
- R1-12 (lit-escape multi-line invertibility): **Rebuttal holds; agree.** Decode rule ("per line, strip exactly ONE `[[gateway-lit:v1]] ` prefix if present") + encode ("any INPUT line that begins with `[[gateway-` (after optional leading whitespace) is emitted prefixed with `[[gateway-lit:v1]] ` (marker, one space, then the original line byte-for-byte)") is a per-line bijection. No block-level state or count is encoded, so multi-line blocks of sentinel-like text decode line-by-line to the exact original byte sequence (including a line that is itself a sentinel, a line already carrying a lit prefix (peels one layer), and arbitrary mixes). Escaping explicitly outside fences only. Matrix 5 now requires the multi-line + lit-only + self-sentinel roundtrips (plus leading note iff any marker). Concrete counterexample: none found after enumerating cases (single-line, nested lit, mixed triggering/non, leading-ws originals, full multi-line blocks). The original concern appears to have assumed block-level encoding that does not exist in the rule.

**Other cross-checks:**
- No `src/compressor/`, no `compressDisplayText`/`CompressResult`/`recordCompressionTelemetry` symbols in src/ (only in the three docs). Compressor does not exist yet.
- All gateway citations (symbols + lines) were re-anchored; where numbers are slightly off (e.g. 8212 vs. actual ~8215), the spec's section 0 convention ("symbol governs") applies and the described behavior matches.
- C1/C7/ordering/FR escape hatch/byte-identity tests in matrix 1/8/9/10/11 are consistent with the described call sites and current pre-compressor paths.
- No contradictions between revised spec, diff, round log, and source. All adopted dispositions are actually present in the revised text (not just summarized). The rebutted item is checkable via the stated rule + test.

**NOTES (non-blocking nits only):**
- Decode rule in 6.4 is functionally correct but could be one sentence tighter on leading whitespace ("lit-prefixed output lines are emitted with no leading ws on the prefix itself; decode strips a leading '[[gateway-lit:v1]] ' occurrence, preserving any original leading ws that followed it").
- A couple of FR line numbers in 5.3 are off-by-a-few from current source (symbol names are accurate).
- These are editorial; none affect correctness, invariants, test matrix, or implementation surface.

VERDICT: APPROVE (unconditional)
