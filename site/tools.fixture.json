{
  "generatedAt": "deterministic build output",
  "packageName": "llm-cli-gateway",
  "packageVersion": "2.17.1",
  "siteVersion": "2.17.1",
  "captureCommand": "node scripts/generate-site-discovery.mjs",
  "source": "runtime MCP tools/list from dist/index.js over in-memory MCP transport",
  "toolCount": 63,
  "tools": [
    {
      "name": "approval_list",
      "description": "List recent MCP-managed approval decisions recorded by the gateway (approvalStrategy: mcp_managed).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 500,
            "default": 50,
            "description": "Max number of approval records"
          },
          "cli": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor"
            ],
            "description": "Optional CLI filter (any gateway CLI provider, derived from CLI_TYPES)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "ask_model",
      "description": "Ask one provider CLI a question through the simplified validation surface (starts a validation job).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "question": {
            "type": "string",
            "minLength": 1,
            "description": "Question for one provider."
          },
          "model": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor"
            ],
            "default": "claude",
            "description": "Provider to ask."
          },
          "select": {
            "type": "string",
            "enum": [
              "cheapest",
              "cheapest_per_tier"
            ],
            "description": "Optional least-cost routing: fill the provider target(s) from the LCR selector instead of the explicit list. 'cheapest' picks the single cheapest eligible provider; 'cheapest_per_tier' picks the cheapest in each quality tier. Requires [least_cost].enabled=true; fails closed (no default-list fallback) when disabled or nothing is eligible."
          }
        },
        "required": [
          "question"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "claude_request",
      "description": "Run a Claude Code CLI request synchronously (when async jobs are enabled, auto-defers to a pollable job past the sync deadline; otherwise runs to completion). Requires exactly one of prompt or promptParts.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for Claude (mutually exclusive with promptParts)"
          },
          "promptParts": {
            "type": "object",
            "properties": {
              "system": {
                "type": "string",
                "maxLength": 100000
              },
              "tools": {
                "type": "string",
                "maxLength": 100000
              },
              "context": {
                "type": "string",
                "maxLength": 100000
              },
              "task": {
                "type": "string",
                "minLength": 1,
                "maxLength": 100000
              },
              "cacheControl": {
                "type": "object",
                "properties": {
                  "system": {
                    "type": "boolean"
                  },
                  "tools": {
                    "type": "boolean"
                  },
                  "context": {
                    "type": "boolean"
                  }
                },
                "additionalProperties": false
              }
            },
            "required": [
              "task"
            ],
            "additionalProperties": false,
            "description": "Cache-aware structured prompt: { system?, tools?, context?, task, cacheControl? }. Use for repeated calls that share a stable prefix — `system`/`tools`/`context` are the stable head; `task` is the volatile tail (never marked). Set `cacheControl: { system?: boolean, tools?: boolean, context?: boolean }` to opt into explicit Anthropic prefix caching via `--input-format stream-json` (slice κ). Requires `outputFormat: 'stream-json'` and hard-codes `ttl='1h'` (Anthropic rejects 5m blocks after Claude Code's 1h-marked session-wrap content). Mutually exclusive with `prompt`. The stable prefix hash is logged to the flight recorder for cache_state aggregates."
          },
          "model": {
            "type": "string",
            "description": "Model name or alias (e.g. sonnet, claude-sonnet-4-5-20250929, latest)"
          },
          "outputFormat": {
            "type": "string",
            "enum": [
              "text",
              "json",
              "stream-json"
            ],
            "default": "stream-json",
            "description": "Output format (text|json|stream-json). DEFAULT: stream-json; the gateway parses NDJSON usage events to extract input/output/cache_read/cache_creation tokens + cost + model, persists them to the flight recorder for cache_state aggregates, and still returns the assistant text. Override to 'text' only when you truly want unparsed stdout (loses observability)."
          },
          "sessionId": {
            "type": "string",
            "minLength": 1,
            "description": "On a fresh request this id is emitted as Claude --session-id <uuid> (must be a valid UUID that does not already exist). Resume the latest cwd conversation with continueSession:true. gw-* ids are not valid Claude --session-id values."
          },
          "continueSession": {
            "type": "boolean",
            "default": false,
            "description": "Continue the most recent Claude conversation in the selected workspace (emits --continue; real CLI continuity). Stable workspace selection is required via workingDir, a registered workspace, or the configured default workspace."
          },
          "createNewSession": {
            "type": "boolean",
            "default": false,
            "description": "Force new session"
          },
          "allowedTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Allowed tools (['Bash(git:*)','Edit','Write'])"
          },
          "disallowedTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Disallowed tools"
          },
          "dangerouslySkipPermissions": {
            "type": "boolean",
            "default": false,
            "description": "DEPRECATED: prefer `permissionMode: \"bypassPermissions\"`. Maps to it when `permissionMode` is unset. Under mcp_managed, either form requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1."
          },
          "permissionMode": {
            "type": "string",
            "enum": [
              "default",
              "acceptEdits",
              "auto",
              "bypassPermissions",
              "manual",
              "dontAsk",
              "plan"
            ],
            "description": "Claude --permission-mode: default|acceptEdits|auto|bypassPermissions|manual|dontAsk|plan. `default` is a no-op (no flag emitted). Under mcp_managed, bypassPermissions requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1."
          },
          "agent": {
            "type": "string",
            "description": "Claude --agent: dispatch to a named single sub-agent."
          },
          "agents": {
            "type": "object",
            "additionalProperties": {},
            "description": "Claude --agents: inline JSON map of agent name → { description, prompt, tools?, model? }."
          },
          "forkSession": {
            "type": "boolean",
            "description": "Claude --fork-session: branch from an existing session into a fresh fork."
          },
          "systemPrompt": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --system-prompt: replace the system prompt entirely. Mutually exclusive with appendSystemPrompt. Under mcp_managed, an override requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1."
          },
          "appendSystemPrompt": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --append-system-prompt: append to the existing system prompt. Mutually exclusive with systemPrompt. Under mcp_managed, an override requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1."
          },
          "maxBudgetUsd": {
            "type": "number",
            "exclusiveMinimum": 0,
            "description": "Claude --max-budget-usd: spend cap for this request in USD."
          },
          "maxTurns": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "description": "Claude --max-turns: cap on agent loop iterations."
          },
          "effort": {
            "type": "string",
            "enum": [
              "low",
              "medium",
              "high",
              "xhigh",
              "max"
            ],
            "description": "Claude --effort: low|medium|high|xhigh|max."
          },
          "excludeDynamicSystemPromptSections": {
            "type": "boolean",
            "description": "Claude --exclude-dynamic-system-prompt-sections: trim dynamic context blocks from the system prompt."
          },
          "fallbackModel": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --fallback-model: model name to auto-fallback to when the default model is overloaded (effective only with --print, which the gateway always uses)."
          },
          "jsonSchema": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "object",
                "additionalProperties": {}
              }
            ],
            "description": "Claude --json-schema: JSON Schema literal (NOT a path) constraining structured output. Object values are JSON.stringify-d; string values are passed verbatim. Use with outputFormat='json'. Set outputFormat:json so the gateway treats the reply as structured output (skips response optimization and warning injection); the default output format is not json, so pass it explicitly."
          },
          "workingDir": {
            "type": "string",
            "minLength": 1,
            "description": "Claude process working directory. The gateway launches Claude in this directory. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "addDir": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Claude --add-dir: additional directories the CLI is allowed to read/write beyond the process cwd. Each entry is emitted as its own --add-dir instance. Under mcp_managed, non-empty addDir requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "noSessionPersistence": {
            "type": "boolean",
            "description": "Claude --no-session-persistence: do not write this session to disk (ephemeral one-shot runs; mirrors codex --ephemeral)."
          },
          "settingSources": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --setting-sources: comma-separated setting sources to load (user|project|local) for reproducible/isolated headless runs."
          },
          "settings": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --settings: path to a settings JSON file or a JSON literal of additional settings. Powerful: settings can define hooks/permissions/model; passed verbatim."
          },
          "tools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Claude --tools: restrict the available built-in tool set (distinct from allowedTools permission gating). Pass [\"\"] to disable all tools."
          },
          "includeHookEvents": {
            "type": "boolean",
            "description": "Claude --include-hook-events: include all hook lifecycle events in the output stream. Only takes effect with outputFormat=stream-json (the default)."
          },
          "replayUserMessages": {
            "type": "boolean",
            "description": "Claude --replay-user-messages: re-emit user messages from stdin back on stdout for acknowledgment. Only works with input-format=stream-json and outputFormat=stream-json (the cacheControl path)."
          },
          "systemPromptFile": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --system-prompt-file: replace the system prompt from a file path (path variant of systemPrompt)."
          },
          "appendSystemPromptFile": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --append-system-prompt-file: append a system prompt from a file path (path variant of appendSystemPrompt)."
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --name: display name for this session (shown in pickers/titles)."
          },
          "pluginDir": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Claude --plugin-dir: load a plugin from a directory or .zip for this session only. One --plugin-dir instance per entry."
          },
          "pluginUrl": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Claude --plugin-url: load a plugin .zip from a URL for this session only. One --plugin-url instance per entry."
          },
          "safeMode": {
            "type": "boolean",
            "description": "Claude --safe-mode: start with all customizations (CLAUDE.md, skills, plugins, hooks, MCP, commands, agents) disabled for troubleshooting. Under mcp_managed, this repository-rule suppression requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1."
          },
          "bare": {
            "type": "boolean",
            "description": "Claude --bare: minimal mode (skip hooks, LSP, plugin sync, attribution, auto-memory, keychain reads, CLAUDE.md auto-discovery). Under mcp_managed, this repository-rule suppression requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1."
          },
          "debug": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "boolean"
              }
            ],
            "description": "Claude -d/--debug: enable debug mode. true emits a bare --debug; a string emits --debug <filter> (e.g. \"api,hooks\"). Debug output goes to stderr only."
          },
          "debugFile": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --debug-file: write debug logs to a specific file path (enables debug mode)."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Do not use this field, workspace_list, or workspace_register_existing_repo as a fallback for stdio/local provider path access; pass workingDir/addDir/includeDirs directly instead."
          },
          "worktree": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 64
                  },
                  "ref": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 255
                  }
                },
                "additionalProperties": false
              }
            ],
            "description": "Slice λ: run this request inside a dedicated git worktree owned by the gateway. `true` creates a fresh worktree at `<repoRoot>/.worktrees/<uuid>` branched from HEAD. `{ name?, ref? }` lets the caller supply a sanitized name and/or a git ref (default: HEAD). When the request carries a sessionId and the session already has a worktree, reuse requires same-host ownership metadata and a matching live Git registration. Named path collisions never reuse manager state. Gateway-managed worktrees require the local file-backed session manager and fail closed with PostgreSQL sessions. The Grok, Devin, and Mistral adapters require an explicit provider-native sessionId; fresh, createNewSession, and resumeLatest-only worktree requests fail closed because they cannot durably reselect the worktree. The request must select a registered workspace explicitly, through caller-owned session metadata, or through the configured default. Worktrees never fall back to the gateway process cwd and cannot be combined with local workingDir, addDir, or includeDirs paths. The gateway spawns the child CLI with `cwd: <worktree-path>`; no `-w`/`--worktree` flag is ever emitted to the underlying CLI. On worktree materialization, the gateway suppresses repository, system, and global Git hooks and configured clean, smudge, and process checkout filters, sparse checkout, and lazy object fetching. Filter-dependent content such as Git LFS remains in its repository representation instead of executing host commands. On session_delete or TTL eviction the gateway hides the session and runs `git worktree remove --force`. Failed removal retains a durable cleanup-pending tombstone which blocks reuse and is retried when the file store is registered on the owning host. The tombstone is finalized only after verified Git removal. Successful responses are prefixed with `[gateway] worktree=<absolute-path>\\n` so callers can use the path. For Claude approvalStrategy:mcp_managed, requesting or reusing a worktree requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1. Other adapters reject mcp_managed before launch. NOTE: callers should `.gitignore` the `.worktrees/` directory in their repo (the gateway does NOT auto-gitignore; see slice λ spec Q4)."
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy: legacy (default) lets Claude's own flags decide; mcp_managed routes the run through the gateway approval gate and uses acceptEdits by default. An explicit bypassPermissions request requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "Approval policy when approvalStrategy is mcp_managed: strict|balanced|permissive (default balanced). Ignored under legacy strategy."
          },
          "mcpServers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "description": "MCP servers exposed to Claude"
          },
          "strictMcpConfig": {
            "type": "boolean",
            "default": false,
            "description": "Restrict Claude to provided MCP config only. mcp_managed always enforces this isolation, even when false is supplied."
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "optimizePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Optimize prompt before execution"
          },
          "optimizeResponse": {
            "type": "boolean",
            "default": false,
            "description": "Optimize response output"
          },
          "compressResponse": {
            "type": "boolean",
            "description": "Compress the response display text via the native compressor (default: [compression].enabled in config.toml, off unless opted in). Skipped for structured output."
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h, omit=CLI default). Idle enforcement applies only when outputFormat is stream-json; it is ignored for text/json."
          },
          "forceRefresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass dedup and force a fresh CLI run even if a recent identical request exists"
          },
          "requestInstructions": {
            "type": "string",
            "maxLength": 16384,
            "description": "Per-request Kit instructions. Available only when [personal_config].enabled = true."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "claude_request_async",
      "description": "Start a Claude Code CLI request as a durable background job. Poll with llm_job_status, collect with llm_job_result.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for Claude (mutually exclusive with promptParts)"
          },
          "promptParts": {
            "type": "object",
            "properties": {
              "system": {
                "type": "string",
                "maxLength": 100000
              },
              "tools": {
                "type": "string",
                "maxLength": 100000
              },
              "context": {
                "type": "string",
                "maxLength": 100000
              },
              "task": {
                "type": "string",
                "minLength": 1,
                "maxLength": 100000
              },
              "cacheControl": {
                "type": "object",
                "properties": {
                  "system": {
                    "type": "boolean"
                  },
                  "tools": {
                    "type": "boolean"
                  },
                  "context": {
                    "type": "boolean"
                  }
                },
                "additionalProperties": false
              }
            },
            "required": [
              "task"
            ],
            "additionalProperties": false,
            "description": "Cache-aware structured prompt: { system?, tools?, context?, task, cacheControl? }. Same semantics as claude_request: stable head (system/tools/context) + volatile tail (task). Set `cacheControl: { system?, tools?, context?: boolean }` to opt into explicit Anthropic prefix caching via `--input-format stream-json` (slice κ); requires `outputFormat: 'stream-json'` and hard-codes `ttl='1h'`. Mutually exclusive with `prompt`. Stable prefix hash logged to flight recorder."
          },
          "model": {
            "type": "string",
            "description": "Model name or alias (e.g. sonnet, claude-sonnet-4-5-20250929, latest)"
          },
          "outputFormat": {
            "type": "string",
            "enum": [
              "text",
              "json",
              "stream-json"
            ],
            "default": "stream-json",
            "description": "Output format (text|json|stream-json). DEFAULT: stream-json — same rationale as claude_request: keeps usage/cache/cost observable for cache_state aggregates. Override to 'text' only when raw stdout is required (loses observability)."
          },
          "sessionId": {
            "type": "string",
            "minLength": 1,
            "description": "On a fresh request this id is emitted as Claude --session-id <uuid> (must be a valid UUID that does not already exist). Resume the latest cwd conversation with continueSession:true. gw-* ids are not valid Claude --session-id values."
          },
          "continueSession": {
            "type": "boolean",
            "default": false,
            "description": "Continue the most recent Claude conversation in the selected workspace (emits --continue; real CLI continuity). Stable workspace selection is required via workingDir, a registered workspace, or the configured default workspace."
          },
          "createNewSession": {
            "type": "boolean",
            "default": false,
            "description": "Force new session"
          },
          "allowedTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Allowed tools (['Bash(git:*)','Edit','Write'])"
          },
          "disallowedTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Disallowed tools"
          },
          "dangerouslySkipPermissions": {
            "type": "boolean",
            "default": false,
            "description": "DEPRECATED: prefer `permissionMode: \"bypassPermissions\"`. Maps to it when `permissionMode` is unset. Under mcp_managed, either form requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1."
          },
          "permissionMode": {
            "type": "string",
            "enum": [
              "default",
              "acceptEdits",
              "auto",
              "bypassPermissions",
              "manual",
              "dontAsk",
              "plan"
            ],
            "description": "Claude --permission-mode: default|acceptEdits|auto|bypassPermissions|manual|dontAsk|plan. `default` is a no-op. Under mcp_managed, bypassPermissions requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1."
          },
          "agent": {
            "type": "string",
            "description": "Claude --agent: dispatch to a named single sub-agent."
          },
          "agents": {
            "type": "object",
            "additionalProperties": {},
            "description": "Claude --agents: inline JSON map of agent name → { description, prompt, tools?, model? }."
          },
          "forkSession": {
            "type": "boolean",
            "description": "Claude --fork-session: branch from an existing session into a fresh fork."
          },
          "systemPrompt": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --system-prompt: replace the system prompt entirely. Mutually exclusive with appendSystemPrompt. Under mcp_managed, an override requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1."
          },
          "appendSystemPrompt": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --append-system-prompt: append to the existing system prompt. Mutually exclusive with systemPrompt. Under mcp_managed, an override requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1."
          },
          "maxBudgetUsd": {
            "type": "number",
            "exclusiveMinimum": 0,
            "description": "Claude --max-budget-usd: spend cap for this request in USD."
          },
          "maxTurns": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "description": "Claude --max-turns: cap on agent loop iterations."
          },
          "effort": {
            "type": "string",
            "enum": [
              "low",
              "medium",
              "high",
              "xhigh",
              "max"
            ],
            "description": "Claude --effort: low|medium|high|xhigh|max."
          },
          "excludeDynamicSystemPromptSections": {
            "type": "boolean",
            "description": "Claude --exclude-dynamic-system-prompt-sections: trim dynamic context blocks from the system prompt."
          },
          "fallbackModel": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --fallback-model: model name to auto-fallback to when the default model is overloaded (effective only with --print, which the gateway always uses)."
          },
          "jsonSchema": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "object",
                "additionalProperties": {}
              }
            ],
            "description": "Claude --json-schema: JSON Schema literal (NOT a path) constraining structured output. Object values are JSON.stringify-d; string values are passed verbatim. Use with outputFormat='json'. Set outputFormat:json so the gateway treats the reply as structured output (skips response optimization and warning injection); the default output format is not json, so pass it explicitly."
          },
          "workingDir": {
            "type": "string",
            "minLength": 1,
            "description": "Claude process working directory. The gateway launches Claude in this directory. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "addDir": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Claude --add-dir: additional directories the CLI is allowed to read/write beyond the process cwd. Each entry is emitted as its own --add-dir instance. Under mcp_managed, non-empty addDir requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "noSessionPersistence": {
            "type": "boolean",
            "description": "Claude --no-session-persistence: do not write this session to disk (ephemeral one-shot runs; mirrors codex --ephemeral)."
          },
          "settingSources": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --setting-sources: comma-separated setting sources to load (user|project|local) for reproducible/isolated headless runs."
          },
          "settings": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --settings: path to a settings JSON file or a JSON literal of additional settings. Powerful: settings can define hooks/permissions/model; passed verbatim."
          },
          "tools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Claude --tools: restrict the available built-in tool set (distinct from allowedTools permission gating). Pass [\"\"] to disable all tools."
          },
          "includeHookEvents": {
            "type": "boolean",
            "description": "Claude --include-hook-events: include all hook lifecycle events in the output stream. Only takes effect with outputFormat=stream-json (the default)."
          },
          "replayUserMessages": {
            "type": "boolean",
            "description": "Claude --replay-user-messages: re-emit user messages from stdin back on stdout for acknowledgment. Only works with input-format=stream-json and outputFormat=stream-json (the cacheControl path)."
          },
          "systemPromptFile": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --system-prompt-file: replace the system prompt from a file path (path variant of systemPrompt)."
          },
          "appendSystemPromptFile": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --append-system-prompt-file: append a system prompt from a file path (path variant of appendSystemPrompt)."
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --name: display name for this session (shown in pickers/titles)."
          },
          "pluginDir": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Claude --plugin-dir: load a plugin from a directory or .zip for this session only. One --plugin-dir instance per entry."
          },
          "pluginUrl": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Claude --plugin-url: load a plugin .zip from a URL for this session only. One --plugin-url instance per entry."
          },
          "safeMode": {
            "type": "boolean",
            "description": "Claude --safe-mode: start with all customizations (CLAUDE.md, skills, plugins, hooks, MCP, commands, agents) disabled for troubleshooting. Under mcp_managed, this repository-rule suppression requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1."
          },
          "bare": {
            "type": "boolean",
            "description": "Claude --bare: minimal mode (skip hooks, LSP, plugin sync, attribution, auto-memory, keychain reads, CLAUDE.md auto-discovery). Under mcp_managed, this repository-rule suppression requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1."
          },
          "debug": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "boolean"
              }
            ],
            "description": "Claude -d/--debug: enable debug mode. true emits a bare --debug; a string emits --debug <filter> (e.g. \"api,hooks\"). Debug output goes to stderr only."
          },
          "debugFile": {
            "type": "string",
            "minLength": 1,
            "description": "Claude --debug-file: write debug logs to a specific file path (enables debug mode)."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Do not use this field, workspace_list, or workspace_register_existing_repo as a fallback for stdio/local provider path access; pass workingDir/addDir/includeDirs directly instead."
          },
          "worktree": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 64
                  },
                  "ref": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 255
                  }
                },
                "additionalProperties": false
              }
            ],
            "description": "Slice λ: run this request inside a dedicated git worktree owned by the gateway. `true` creates a fresh worktree at `<repoRoot>/.worktrees/<uuid>` branched from HEAD. `{ name?, ref? }` lets the caller supply a sanitized name and/or a git ref (default: HEAD). When the request carries a sessionId and the session already has a worktree, reuse requires same-host ownership metadata and a matching live Git registration. Named path collisions never reuse manager state. Gateway-managed worktrees require the local file-backed session manager and fail closed with PostgreSQL sessions. The Grok, Devin, and Mistral adapters require an explicit provider-native sessionId; fresh, createNewSession, and resumeLatest-only worktree requests fail closed because they cannot durably reselect the worktree. The request must select a registered workspace explicitly, through caller-owned session metadata, or through the configured default. Worktrees never fall back to the gateway process cwd and cannot be combined with local workingDir, addDir, or includeDirs paths. The gateway spawns the child CLI with `cwd: <worktree-path>`; no `-w`/`--worktree` flag is ever emitted to the underlying CLI. On worktree materialization, the gateway suppresses repository, system, and global Git hooks and configured clean, smudge, and process checkout filters, sparse checkout, and lazy object fetching. Filter-dependent content such as Git LFS remains in its repository representation instead of executing host commands. On session_delete or TTL eviction the gateway hides the session and runs `git worktree remove --force`. Failed removal retains a durable cleanup-pending tombstone which blocks reuse and is retried when the file store is registered on the owning host. The tombstone is finalized only after verified Git removal. Successful responses are prefixed with `[gateway] worktree=<absolute-path>\\n` so callers can use the path. For Claude approvalStrategy:mcp_managed, requesting or reusing a worktree requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1. Other adapters reject mcp_managed before launch. NOTE: callers should `.gitignore` the `.worktrees/` directory in their repo (the gateway does NOT auto-gitignore; see slice λ spec Q4)."
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy: legacy (default) lets Claude's own flags decide; mcp_managed routes the run through the gateway approval gate and uses acceptEdits by default. An explicit bypassPermissions request requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "Approval policy when approvalStrategy is mcp_managed: strict|balanced|permissive (default balanced). Ignored under legacy strategy."
          },
          "mcpServers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "description": "MCP servers exposed to Claude"
          },
          "strictMcpConfig": {
            "type": "boolean",
            "default": false,
            "description": "Restrict Claude to provided MCP config only. mcp_managed always enforces this isolation, even when false is supplied."
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "optimizePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Optimize prompt before execution"
          },
          "compressResponse": {
            "type": "boolean",
            "description": "Compress the response display text when collected via llm_job_result (native compressor; default: [compression].enabled)."
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h, omit=CLI default). Idle enforcement applies only when outputFormat is stream-json; it is ignored for text/json."
          },
          "forceRefresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass dedup and force a fresh CLI run even if a recent identical request exists"
          },
          "requestInstructions": {
            "type": "string",
            "maxLength": 16384,
            "description": "Per-request Kit instructions. Available only when [personal_config].enabled = true."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cli_upgrade",
      "description": "Plan (dryRun, default true) or execute an upgrade for one provider CLI using its native update mechanism.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "cli": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor"
            ],
            "description": "CLI to upgrade (claude|codex|gemini|grok|mistral|devin|cursor)"
          },
          "target": {
            "type": "string",
            "minLength": 1,
            "default": "latest",
            "description": "Package tag/version/target to install (default: latest)"
          },
          "dryRun": {
            "type": "boolean",
            "default": true,
            "description": "When true, return the upgrade plan without running it"
          },
          "timeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Upgrade timeout in ms when dryRun=false"
          }
        },
        "required": [
          "cli"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cli_versions",
      "description": "Report installed provider CLI versions, availability, and login status for all registered CLI providers (claude|codex|gemini|grok|mistral|devin|cursor) or one.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "cli": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor"
            ],
            "description": "CLI filter (claude|codex|gemini|grok|mistral|devin|cursor)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "codex_fork_session",
      "description": "Fork an existing Codex session into a new branch (codex fork <ID|--last>) without mutating the original. This prompt remains argv-bound and rejects oversized UTF-8 input as non-retryable input_too_large.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for the forked Codex session. This codex fork path remains argv-bound and rejects oversized UTF-8 input."
          },
          "sessionId": {
            "type": "string",
            "minLength": 1,
            "description": "Codex session UUID to fork from. Mutually exclusive with `forkLast`."
          },
          "forkLast": {
            "type": "boolean",
            "description": "Fork from the most recent Codex session. Mutually exclusive with `sessionId`."
          },
          "model": {
            "type": "string",
            "description": "Model name or alias (e.g. gpt-5.5, latest)"
          },
          "sandboxMode": {
            "type": "string",
            "enum": [
              "read-only",
              "workspace-write",
              "danger-full-access"
            ],
            "description": "Codex --sandbox. Omit = Codex exec built-in default (read-only; cannot write files). Pass workspace-write to let Codex edit files in the working dir, or danger-full-access for unrestricted access."
          },
          "askForApproval": {
            "type": "string",
            "enum": [
              "untrusted",
              "on-request",
              "never"
            ],
            "description": "DEPRECATED compatibility input: accepted but ignored because current Codex no longer accepts --ask-for-approval."
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy: legacy is supported. mcp_managed is rejected before launch because this adapter cannot isolate ambient MCP configuration."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "approvalPolicy is unavailable for this provider. It has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h, omit=CLI default)"
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Do not use this field, workspace_list, or workspace_register_existing_repo as a fallback for stdio/local provider path access; pass workingDir/addDir/includeDirs directly instead."
          }
        },
        "required": [
          "prompt"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "codex_request",
      "description": "Run an OpenAI Codex CLI request synchronously (when async jobs are enabled, auto-defers to a pollable job past the sync deadline; otherwise runs to completion). Requires exactly one of prompt or promptParts.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for Codex (mutually exclusive with promptParts)"
          },
          "promptParts": {
            "type": "object",
            "properties": {
              "system": {
                "type": "string",
                "maxLength": 100000
              },
              "tools": {
                "type": "string",
                "maxLength": 100000
              },
              "context": {
                "type": "string",
                "maxLength": 100000
              },
              "task": {
                "type": "string",
                "minLength": 1,
                "maxLength": 100000
              },
              "cacheControl": {
                "type": "object",
                "properties": {
                  "system": {
                    "type": "boolean"
                  },
                  "tools": {
                    "type": "boolean"
                  },
                  "context": {
                    "type": "boolean"
                  }
                },
                "additionalProperties": false
              }
            },
            "required": [
              "task"
            ],
            "additionalProperties": false,
            "description": "Cache-aware structured prompt: { system?, tools?, context?, task }. Mutually exclusive with prompt. Stable parts hash into cache_state for prefix-discipline tracking."
          },
          "model": {
            "type": "string",
            "description": "Model name or alias (e.g. gpt-5.5, gpt-5.4, latest)"
          },
          "fullAuto": {
            "type": "boolean",
            "default": false,
            "description": "DEPRECATED: prefer `sandboxMode`. Expands to `--sandbox workspace-write`; current Codex no longer accepts approval-policy flags."
          },
          "sandboxMode": {
            "type": "string",
            "enum": [
              "read-only",
              "workspace-write",
              "danger-full-access"
            ],
            "description": "Codex --sandbox. Omit = Codex exec built-in default (read-only; cannot write files). Pass workspace-write to let Codex edit files in the working dir, or danger-full-access for unrestricted access."
          },
          "askForApproval": {
            "type": "string",
            "enum": [
              "untrusted",
              "on-request",
              "never"
            ],
            "description": "DEPRECATED compatibility input: accepted but ignored because current Codex no longer accepts --ask-for-approval."
          },
          "useLegacyFullAutoFlag": {
            "type": "boolean",
            "default": false,
            "description": "DEPRECATED compatibility input: accepted but ignored because current Codex no longer accepts --full-auto."
          },
          "dangerouslyBypassApprovalsAndSandbox": {
            "type": "boolean",
            "default": false,
            "description": "Run Codex without approvals/sandbox."
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy: legacy is supported. mcp_managed is rejected before launch because this adapter cannot isolate ambient MCP configuration."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "approvalPolicy is unavailable for this provider. It has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "mcpServers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "description": "This provider does not receive gateway-managed MCP configuration. mcpServers has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "sessionId": {
            "type": "string",
            "minLength": 1,
            "description": "Codex session UUID to resume via `codex exec resume <ID>`. Must be a real Codex session ID (from `~/.codex/sessions/` or the `codex resume` picker). Gateway-generated `gw-*` IDs are rejected. For a brand-new session no resumable sessionId is returned; continue with resumeLatest:true or a real Codex UUID."
          },
          "resumeLatest": {
            "type": "boolean",
            "default": false,
            "description": "Resume Codex's globally latest session via `codex exec resume --last`; it inherits that session's original cwd, and workingDir/addDir do not retarget it. Ignored if sessionId is set; an explicit real Codex UUID targets that session. A brand-new session returns no resumable sessionId; continue with resumeLatest:true or a real Codex UUID."
          },
          "createNewSession": {
            "type": "boolean",
            "default": false,
            "description": "Force a fresh session (no resume)"
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "optimizePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Optimize prompt before execution"
          },
          "optimizeResponse": {
            "type": "boolean",
            "default": false,
            "description": "Optimize response output"
          },
          "compressResponse": {
            "type": "boolean",
            "description": "Compress the response display text via the native compressor (default: [compression].enabled in config.toml, off unless opted in). Skipped for structured output."
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h, omit=CLI default)"
          },
          "forceRefresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass dedup and force a fresh CLI run even if a recent identical request exists"
          },
          "requestInstructions": {
            "type": "string",
            "maxLength": 16384,
            "description": "Per-request Kit instructions. Available only when [personal_config].enabled = true."
          },
          "outputFormat": {
            "type": "string",
            "enum": [
              "text",
              "json"
            ],
            "description": "Codex caller-facing output format. Token/cache usage is recorded in the flight recorder regardless. `text` (default) returns the plain reply; `json` returns the raw `--json` JSONL event stream."
          },
          "outputSchema": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "object",
                "additionalProperties": {}
              }
            ],
            "description": "Codex --output-schema. Pass a path (string) or an inline JSON Schema object; object is materialised to a 0o600 temp file under os.tmpdir() and deleted after the run."
          },
          "search": {
            "type": "boolean",
            "description": "DEPRECATED compatibility input: accepted but ignored because current Codex exec no longer accepts --search."
          },
          "profile": {
            "type": "string",
            "description": "Codex --profile <name>: select a profile from ~/.codex/config.toml."
          },
          "configOverrides": {
            "type": "object",
            "additionalProperties": {},
            "description": "Codex -c key=value overrides. Keys: /^[a-zA-Z0-9._]+$/. Values: no CR/LF. Local callers only, remote HTTP/OAuth requests are rejected."
          },
          "ephemeral": {
            "type": "boolean",
            "description": "Codex --ephemeral: do not persist the session to disk."
          },
          "images": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Codex -i <path>: image attachments. Each path must exist; missing paths fail fast."
          },
          "ignoreUserConfig": {
            "type": "boolean",
            "description": "Codex --ignore-user-config: ignore ~/.codex/config.toml for this run."
          },
          "ignoreRules": {
            "type": "boolean",
            "description": "Codex --ignore-rules: skip project rule files for this run."
          },
          "workingDir": {
            "type": "string",
            "minLength": 1,
            "description": "Codex -C/--cd <DIR>: working root for this session. Emitted on new sessions only; resume inherits the original session's cwd via CODEX_RESUME_FILTERED_FLAGS. Personal Agent Config Kit mode requires an absolute path. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "addDir": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Codex --add-dir <DIR>: additional writable workspace directories. Emitted once per entry on new sessions only; resume inherits the original session's writable-dir policy. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "enable": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Codex --enable <FEATURE> (repeatable): enable a feature for this run (equivalent to -c features.<name>=true). Accepted on resume. Local callers only, remote HTTP/OAuth requests are rejected."
          },
          "disable": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Codex --disable <FEATURE> (repeatable): disable a feature for this run (equivalent to -c features.<name>=false). Accepted on resume. Local callers only, remote HTTP/OAuth requests are rejected."
          },
          "strictConfig": {
            "type": "boolean",
            "description": "Codex --strict-config: error out when config.toml contains fields not recognized by this Codex version. New sessions only."
          },
          "oss": {
            "type": "boolean",
            "description": "Codex --oss: use the open-source provider. New sessions only."
          },
          "localProvider": {
            "type": "string",
            "enum": [
              "lmstudio",
              "ollama"
            ],
            "description": "Codex --local-provider: local OSS provider (lmstudio|ollama), used with oss. New sessions only."
          },
          "color": {
            "type": "string",
            "enum": [
              "always",
              "never",
              "auto"
            ],
            "description": "Codex --color: output color mode (always|never|auto). New sessions only."
          },
          "outputLastMessage": {
            "type": "string",
            "minLength": 1,
            "description": "Codex -o/--output-last-message <FILE>: write the agent's last message to a file. New sessions only."
          },
          "dangerouslyBypassHookTrust": {
            "type": "boolean",
            "description": "Codex --dangerously-bypass-hook-trust: run enabled hooks without persisted hook trust for this invocation. DANGEROUS."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Do not use this field, workspace_list, or workspace_register_existing_repo as a fallback for stdio/local provider path access; pass workingDir/addDir/includeDirs directly instead."
          },
          "worktree": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 64
                  },
                  "ref": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 255
                  }
                },
                "additionalProperties": false
              }
            ],
            "description": "Slice λ: run this request inside a dedicated git worktree owned by the gateway. `true` creates a fresh worktree at `<repoRoot>/.worktrees/<uuid>` branched from HEAD. `{ name?, ref? }` lets the caller supply a sanitized name and/or a git ref (default: HEAD). When the request carries a sessionId and the session already has a worktree, reuse requires same-host ownership metadata and a matching live Git registration. Named path collisions never reuse manager state. Gateway-managed worktrees require the local file-backed session manager and fail closed with PostgreSQL sessions. The Grok, Devin, and Mistral adapters require an explicit provider-native sessionId; fresh, createNewSession, and resumeLatest-only worktree requests fail closed because they cannot durably reselect the worktree. The request must select a registered workspace explicitly, through caller-owned session metadata, or through the configured default. Worktrees never fall back to the gateway process cwd and cannot be combined with local workingDir, addDir, or includeDirs paths. The gateway spawns the child CLI with `cwd: <worktree-path>`; no `-w`/`--worktree` flag is ever emitted to the underlying CLI. On worktree materialization, the gateway suppresses repository, system, and global Git hooks and configured clean, smudge, and process checkout filters, sparse checkout, and lazy object fetching. Filter-dependent content such as Git LFS remains in its repository representation instead of executing host commands. On session_delete or TTL eviction the gateway hides the session and runs `git worktree remove --force`. Failed removal retains a durable cleanup-pending tombstone which blocks reuse and is retried when the file store is registered on the owning host. The tombstone is finalized only after verified Git removal. Successful responses are prefixed with `[gateway] worktree=<absolute-path>\\n` so callers can use the path. For Claude approvalStrategy:mcp_managed, requesting or reusing a worktree requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1. Other adapters reject mcp_managed before launch. NOTE: callers should `.gitignore` the `.worktrees/` directory in their repo (the gateway does NOT auto-gitignore; see slice λ spec Q4)."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "codex_request_async",
      "description": "Start an OpenAI Codex CLI request as a durable background job. Poll with llm_job_status, collect with llm_job_result.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for Codex (mutually exclusive with promptParts)"
          },
          "promptParts": {
            "type": "object",
            "properties": {
              "system": {
                "type": "string",
                "maxLength": 100000
              },
              "tools": {
                "type": "string",
                "maxLength": 100000
              },
              "context": {
                "type": "string",
                "maxLength": 100000
              },
              "task": {
                "type": "string",
                "minLength": 1,
                "maxLength": 100000
              },
              "cacheControl": {
                "type": "object",
                "properties": {
                  "system": {
                    "type": "boolean"
                  },
                  "tools": {
                    "type": "boolean"
                  },
                  "context": {
                    "type": "boolean"
                  }
                },
                "additionalProperties": false
              }
            },
            "required": [
              "task"
            ],
            "additionalProperties": false,
            "description": "Cache-aware structured prompt: { system?, tools?, context?, task }. Mutually exclusive with prompt. Stable parts hash into cache_state for prefix-discipline tracking."
          },
          "model": {
            "type": "string",
            "description": "Model name or alias (e.g. gpt-5.5, gpt-5.4, latest)"
          },
          "fullAuto": {
            "type": "boolean",
            "default": false,
            "description": "DEPRECATED: prefer `sandboxMode`. Expands to `--sandbox workspace-write`; current Codex no longer accepts approval-policy flags."
          },
          "sandboxMode": {
            "type": "string",
            "enum": [
              "read-only",
              "workspace-write",
              "danger-full-access"
            ],
            "description": "Codex --sandbox. Omit = Codex exec built-in default (read-only; cannot write files). Pass workspace-write to let Codex edit files in the working dir, or danger-full-access for unrestricted access."
          },
          "askForApproval": {
            "type": "string",
            "enum": [
              "untrusted",
              "on-request",
              "never"
            ],
            "description": "DEPRECATED compatibility input: accepted but ignored because current Codex no longer accepts --ask-for-approval."
          },
          "useLegacyFullAutoFlag": {
            "type": "boolean",
            "default": false,
            "description": "DEPRECATED compatibility input: accepted but ignored because current Codex no longer accepts --full-auto."
          },
          "dangerouslyBypassApprovalsAndSandbox": {
            "type": "boolean",
            "default": false,
            "description": "Run Codex without approvals/sandbox."
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy: legacy is supported. mcp_managed is rejected before launch because this adapter cannot isolate ambient MCP configuration."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "approvalPolicy is unavailable for this provider. It has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "mcpServers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "description": "This provider does not receive gateway-managed MCP configuration. mcpServers has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "sessionId": {
            "type": "string",
            "minLength": 1,
            "description": "Codex session UUID to resume via `codex exec resume <ID>`. Must be a real Codex session ID (from `~/.codex/sessions/` or the `codex resume` picker). Gateway-generated `gw-*` IDs are rejected. For a brand-new session no resumable sessionId is returned; continue with resumeLatest:true or a real Codex UUID."
          },
          "resumeLatest": {
            "type": "boolean",
            "default": false,
            "description": "Resume Codex's globally latest session via `codex exec resume --last`; it inherits that session's original cwd, and workingDir/addDir do not retarget it. Ignored if sessionId is set; an explicit real Codex UUID targets that session. A brand-new session returns no resumable sessionId; continue with resumeLatest:true or a real Codex UUID."
          },
          "createNewSession": {
            "type": "boolean",
            "default": false,
            "description": "Force a fresh session (no resume)"
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "optimizePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Optimize prompt before execution"
          },
          "compressResponse": {
            "type": "boolean",
            "description": "Compress the response display text when collected via llm_job_result (native compressor; default: [compression].enabled)."
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h, omit=CLI default)"
          },
          "forceRefresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass dedup and force a fresh CLI run even if a recent identical request exists"
          },
          "outputFormat": {
            "type": "string",
            "enum": [
              "text",
              "json"
            ],
            "description": "Codex caller-facing output format. Token/cache usage is recorded in the flight recorder regardless. `text` (default) returns the plain reply; `json` returns the raw `--json` JSONL event stream."
          },
          "outputSchema": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "object",
                "additionalProperties": {}
              }
            ],
            "description": "Codex --output-schema. Pass a path (string) or an inline JSON Schema object."
          },
          "search": {
            "type": "boolean",
            "description": "DEPRECATED compatibility input: accepted but ignored because current Codex exec no longer accepts --search."
          },
          "profile": {
            "type": "string",
            "description": "Codex --profile <name>."
          },
          "configOverrides": {
            "type": "object",
            "additionalProperties": {},
            "description": "Codex -c key=value overrides. Keys: /^[a-zA-Z0-9._]+$/. Values: no CR/LF. Local callers only, remote HTTP/OAuth requests are rejected."
          },
          "ephemeral": {
            "type": "boolean",
            "description": "Codex --ephemeral."
          },
          "images": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Codex -i <path>: image attachments."
          },
          "ignoreUserConfig": {
            "type": "boolean",
            "description": "Codex --ignore-user-config."
          },
          "ignoreRules": {
            "type": "boolean",
            "description": "Codex --ignore-rules."
          },
          "workingDir": {
            "type": "string",
            "minLength": 1,
            "description": "Codex -C/--cd <DIR>: working root for this session. New sessions only; resume inherits the original session's cwd. Personal Agent Config Kit mode requires an absolute path. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "addDir": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Codex --add-dir <DIR>: additional writable workspace directories (repeat per entry). New sessions only. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "enable": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Codex --enable <FEATURE> (repeatable): enable a feature for this run (equivalent to -c features.<name>=true). Accepted on resume. Local callers only, remote HTTP/OAuth requests are rejected."
          },
          "disable": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Codex --disable <FEATURE> (repeatable): disable a feature for this run (equivalent to -c features.<name>=false). Accepted on resume. Local callers only, remote HTTP/OAuth requests are rejected."
          },
          "strictConfig": {
            "type": "boolean",
            "description": "Codex --strict-config: error out when config.toml contains fields not recognized by this Codex version. New sessions only."
          },
          "oss": {
            "type": "boolean",
            "description": "Codex --oss: use the open-source provider. New sessions only."
          },
          "localProvider": {
            "type": "string",
            "enum": [
              "lmstudio",
              "ollama"
            ],
            "description": "Codex --local-provider: local OSS provider (lmstudio|ollama), used with oss. New sessions only."
          },
          "color": {
            "type": "string",
            "enum": [
              "always",
              "never",
              "auto"
            ],
            "description": "Codex --color: output color mode (always|never|auto). New sessions only."
          },
          "outputLastMessage": {
            "type": "string",
            "minLength": 1,
            "description": "Codex -o/--output-last-message <FILE>: write the agent's last message to a file. New sessions only."
          },
          "dangerouslyBypassHookTrust": {
            "type": "boolean",
            "description": "Codex --dangerously-bypass-hook-trust: run enabled hooks without persisted hook trust for this invocation. DANGEROUS."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Do not use this field, workspace_list, or workspace_register_existing_repo as a fallback for stdio/local provider path access; pass workingDir/addDir/includeDirs directly instead."
          },
          "worktree": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 64
                  },
                  "ref": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 255
                  }
                },
                "additionalProperties": false
              }
            ],
            "description": "Slice λ: run this request inside a dedicated git worktree owned by the gateway. `true` creates a fresh worktree at `<repoRoot>/.worktrees/<uuid>` branched from HEAD. `{ name?, ref? }` lets the caller supply a sanitized name and/or a git ref (default: HEAD). When the request carries a sessionId and the session already has a worktree, reuse requires same-host ownership metadata and a matching live Git registration. Named path collisions never reuse manager state. Gateway-managed worktrees require the local file-backed session manager and fail closed with PostgreSQL sessions. The Grok, Devin, and Mistral adapters require an explicit provider-native sessionId; fresh, createNewSession, and resumeLatest-only worktree requests fail closed because they cannot durably reselect the worktree. The request must select a registered workspace explicitly, through caller-owned session metadata, or through the configured default. Worktrees never fall back to the gateway process cwd and cannot be combined with local workingDir, addDir, or includeDirs paths. The gateway spawns the child CLI with `cwd: <worktree-path>`; no `-w`/`--worktree` flag is ever emitted to the underlying CLI. On worktree materialization, the gateway suppresses repository, system, and global Git hooks and configured clean, smudge, and process checkout filters, sparse checkout, and lazy object fetching. Filter-dependent content such as Git LFS remains in its repository representation instead of executing host commands. On session_delete or TTL eviction the gateway hides the session and runs `git worktree remove --force`. Failed removal retains a durable cleanup-pending tombstone which blocks reuse and is retried when the file store is registered on the owning host. The tombstone is finalized only after verified Git removal. Successful responses are prefixed with `[gateway] worktree=<absolute-path>\\n` so callers can use the path. For Claude approvalStrategy:mcp_managed, requesting or reusing a worktree requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1. Other adapters reject mcp_managed before launch. NOTE: callers should `.gitignore` the `.worktrees/` directory in their repo (the gateway does NOT auto-gitignore; see slice λ spec Q4)."
          },
          "requestInstructions": {
            "type": "string",
            "maxLength": 16384,
            "description": "Per-request Kit instructions. Available only when [personal_config].enabled = true."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "compare_answers",
      "description": "Summarize agreement/differences between caller-provided answers LOCALLY — does not call any provider.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "question": {
            "type": "string",
            "minLength": 1,
            "description": "Question the answers respond to."
          },
          "answers": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            },
            "minItems": 2,
            "description": "Two or more answers to compare."
          }
        },
        "required": [
          "question",
          "answers"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "config_ack_stale",
      "description": "Acknowledge the current stale Personal Agent Config release for at most 24 hours.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "config_init",
      "description": "Initialize or clone the local Personal Agent Config baseline. This never activates a release by itself.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "remote": {
            "type": "string",
            "minLength": 1,
            "description": "Optional private Git remote to clone"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "config_publish",
      "description": "Push the clean local Personal Agent Config baseline to its configured Git upstream without force-pushing.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "config_recover_kit_attempt",
      "description": "Fence and release one exact unadmitted durable Kit attempt after the previous gateway process has been stopped. Copy the execution and attempt identity from local session_get output. This action is local-only and cannot recover an existing durable job.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "provider": {
            "type": "string",
            "enum": [
              "claude",
              "codex"
            ]
          },
          "sessionId": {
            "type": "string",
            "format": "uuid"
          },
          "attemptId": {
            "type": "string",
            "format": "uuid"
          },
          "execution": {
            "type": "object",
            "properties": {
              "version": {
                "type": "number",
                "const": 1
              },
              "releaseId": {
                "type": "string",
                "pattern": "^(?:[a-f0-9]{40}|[a-f0-9]{64})$"
              },
              "configStamp": {
                "type": "string",
                "pattern": "^[a-f0-9]{64}$"
              },
              "scopeRoot": {
                "anyOf": [
                  {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 4096
                  },
                  {
                    "type": "null"
                  }
                ]
              },
              "scopeHead": {
                "anyOf": [
                  {
                    "type": "string",
                    "pattern": "^(?:[a-f0-9]{40}|[a-f0-9]{64})$"
                  },
                  {
                    "type": "null"
                  }
                ]
              },
              "contextIdentity": {
                "type": "string",
                "pattern": "^[a-f0-9]{64}$"
              }
            },
            "required": [
              "version",
              "releaseId",
              "configStamp",
              "scopeRoot",
              "scopeHead",
              "contextIdentity"
            ],
            "additionalProperties": false
          },
          "acknowledgement": {
            "type": "string",
            "const": "I_CONFIRM_THE_PREVIOUS_GATEWAY_IS_STOPPED"
          }
        },
        "required": [
          "provider",
          "sessionId",
          "attemptId",
          "execution",
          "acknowledgement"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "config_rollback",
      "description": "Atomically activate an already verified retained Personal Agent Config release.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "releaseId": {
            "type": "string",
            "pattern": "^(?:[a-f0-9]{40}|[a-f0-9]{64})$"
          }
        },
        "required": [
          "releaseId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "config_status",
      "description": "Show Personal Agent Config state without exposing baseline paths or local machine binding values.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "config_sync",
      "description": "Synchronize, verify, compile, and atomically activate a Personal Agent Config release. It never pushes.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "consensus_check",
      "description": "Ask provider CLIs whether they agree or disagree with a claim (starts validation jobs).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "claim": {
            "type": "string",
            "minLength": 1,
            "description": "Claim to check across providers."
          },
          "models": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "claude",
                "codex",
                "gemini",
                "grok",
                "mistral",
                "devin",
                "cursor"
              ]
            },
            "minItems": 1,
            "default": [
              "claude",
              "codex"
            ],
            "description": "Providers to ask for agreement or disagreement."
          },
          "select": {
            "type": "string",
            "enum": [
              "cheapest",
              "cheapest_per_tier"
            ],
            "description": "Optional least-cost routing: fill the provider target(s) from the LCR selector instead of the explicit list. 'cheapest' picks the single cheapest eligible provider; 'cheapest_per_tier' picks the cheapest in each quality tier. Requires [least_cost].enabled=true; fails closed (no default-list fallback) when disabled or nothing is eligible."
          }
        },
        "required": [
          "claim"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cursor_request",
      "description": "Run a Cursor Agent request synchronously (auto-defers to a pollable job past the sync deadline when async jobs are enabled; otherwise runs to completion). Default `cli` uses headless print mode (`cursor-agent --print`); gated `acp` uses native `cursor-agent acp` and accepts prompt, model, a gateway ACP session, and a registered workspace alias.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for Cursor Agent CLI"
          },
          "model": {
            "type": "string",
            "description": "Model name or alias passed via --model"
          },
          "mode": {
            "type": "string",
            "enum": [
              "plan",
              "ask"
            ],
            "description": "Cursor execution mode: plan (read-only planning) or ask (Q&A/read-only)"
          },
          "outputFormat": {
            "type": "string",
            "enum": [
              "text",
              "json",
              "stream-json"
            ],
            "default": "text",
            "description": "Cursor --output-format for --print mode"
          },
          "transport": {
            "type": "string",
            "enum": [
              "cli",
              "acp"
            ],
            "default": "cli",
            "description": "Transport selector. Default `cli` uses cursor-agent --print. `acp` uses native cursor-agent acp and fails closed unless [acp].enabled plus [acp.providers.cursor].enabled and runtime_enabled are true. ACP accepts prompt, model, a gateway ACP sessionId, and a registered workspace alias; Cursor CLI-only controls are rejected."
          },
          "force": {
            "type": "boolean",
            "default": false,
            "description": "Emit --force (Cursor yolo mode; auto-allows commands unless explicitly denied)"
          },
          "autoReview": {
            "type": "boolean",
            "default": false,
            "description": "Emit --auto-review (Cursor Smart Auto classifier for tool calls)"
          },
          "sandbox": {
            "type": "string",
            "enum": [
              "enabled",
              "disabled"
            ],
            "description": "Cursor sandbox mode override (--sandbox enabled|disabled)"
          },
          "trust": {
            "type": "boolean",
            "default": false,
            "description": "Trust the workspace in headless mode (--trust)"
          },
          "workspace": {
            "type": "string",
            "description": "Workspace directory or saved workspace name (--workspace) on transport=cli. transport=acp requires a registered gateway workspace alias. Remote HTTP/OAuth callers must pass an alias; local callers may pass local Cursor workspace paths only on transport=cli."
          },
          "addDir": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Additional workspace root directories (--add-dir, repeatable)."
          },
          "sessionId": {
            "type": "string",
            "description": "On transport=cli, Cursor chat/session ID to resume (emits --resume <id>); gateway gw-* tracking ids are not CLI session ids. On transport=acp, gateway-owned ACP session ID returned by an earlier ACP call; Cursor-native and CLI session ids are rejected."
          },
          "resumeLatest": {
            "type": "boolean",
            "default": false,
            "description": "Resume the latest Cursor chat (--continue) on transport=cli. true is rejected on transport=acp."
          },
          "createNewSession": {
            "type": "boolean",
            "default": false,
            "description": "Force a new session on transport=cli. true is rejected on transport=acp."
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy. legacy is the only executable CLI strategy; mcp_managed is rejected before Cursor launches because the CLI adapter cannot isolate ambient MCP configuration. transport=acp has its own permission bridge and also rejects mcp_managed."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "On transport=cli, approvalPolicy has no effect because mcp_managed is unavailable. On transport=acp, supplying approvalPolicy is rejected because ACP has its own permission bridge."
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "optimizePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Optimize prompt before execution"
          },
          "optimizeResponse": {
            "type": "boolean",
            "default": false,
            "description": "Optimize response output"
          },
          "compressResponse": {
            "type": "boolean",
            "description": "Compress the response display text via the native compressor (default: [compression].enabled in config.toml, off unless opted in). Skipped for structured output."
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h, omit=CLI default)"
          },
          "forceRefresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass dedup and force a fresh CLI run even if a recent identical request exists"
          }
        },
        "required": [
          "prompt"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "cursor_request_async",
      "description": "Start a Cursor Agent CLI request as a durable background job. Poll with llm_job_status, collect with llm_job_result.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for Cursor Agent CLI"
          },
          "model": {
            "type": "string",
            "description": "Model name or alias passed via --model"
          },
          "mode": {
            "type": "string",
            "enum": [
              "plan",
              "ask"
            ],
            "description": "Cursor execution mode: plan (read-only planning) or ask (Q&A/read-only)"
          },
          "outputFormat": {
            "type": "string",
            "enum": [
              "text",
              "json",
              "stream-json"
            ],
            "default": "text",
            "description": "Cursor --output-format for --print mode"
          },
          "force": {
            "type": "boolean",
            "default": false,
            "description": "Emit --force (Cursor yolo mode; auto-allows commands unless explicitly denied)"
          },
          "autoReview": {
            "type": "boolean",
            "default": false,
            "description": "Emit --auto-review (Cursor Smart Auto classifier for tool calls)"
          },
          "sandbox": {
            "type": "string",
            "enum": [
              "enabled",
              "disabled"
            ],
            "description": "Cursor sandbox mode override (--sandbox enabled|disabled)"
          },
          "trust": {
            "type": "boolean",
            "default": false,
            "description": "Trust the workspace in headless mode"
          },
          "workspace": {
            "type": "string",
            "description": "Workspace directory or saved workspace name (--workspace). This async tool always uses the CLI transport; remote HTTP/OAuth callers must pass a registered workspace alias, while local callers may pass local Cursor workspace paths."
          },
          "addDir": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Additional workspace root directories (--add-dir, repeatable)."
          },
          "sessionId": {
            "type": "string",
            "description": "Cursor chat/session ID to resume (emits --resume <id>). Note: the gw-* id minted for a brand-new gateway session is not resumable via sessionId; continue with resumeLatest:true."
          },
          "resumeLatest": {
            "type": "boolean",
            "default": false,
            "description": "Resume the latest Cursor chat (--continue). Note: the gw-* id minted for a brand-new gateway session is not resumable via sessionId; continue with resumeLatest:true."
          },
          "createNewSession": {
            "type": "boolean",
            "default": false,
            "description": "Force a new session"
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy: legacy is supported. mcp_managed is rejected before launch because this adapter cannot isolate ambient MCP configuration."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "approvalPolicy is unavailable for this provider. It has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "optimizePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Optimize prompt before execution"
          },
          "compressResponse": {
            "type": "boolean",
            "description": "Compress the response display text when collected via llm_job_result (native compressor; default: [compression].enabled)."
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h, omit=CLI default)"
          },
          "forceRefresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass dedup and force a fresh CLI run even if a recent identical request exists"
          }
        },
        "required": [
          "prompt"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "devin_request",
      "description": "Run a Cognition Devin CLI request synchronously (auto-defers to a pollable job past the sync deadline when async jobs are enabled; otherwise runs to completion). Headless print mode (`devin -p`).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for Devin CLI. Required in practice; promptFile is additive (loads an initial prompt from a file)."
          },
          "model": {
            "type": "string",
            "description": "Model name or alias (e.g. opus, latest)"
          },
          "transport": {
            "type": "string",
            "enum": [
              "cli",
              "acp"
            ],
            "default": "cli",
            "description": "Transport: 'cli' (default) runs the Devin CLI. 'acp' routes through `devin acp` only when [acp].enabled plus [acp.providers.devin].enabled and runtime_enabled are true (fails closed otherwise). ACP accepts prompt, model, a gateway ACP sessionId, and the validated agentType; Devin CLI-only controls are rejected."
          },
          "permissionMode": {
            "type": "string",
            "enum": [
              "auto",
              "accept-edits",
              "smart",
              "dangerous"
            ],
            "description": "Devin CLI permission mode (--permission-mode). auto auto-approves read-only tools; accept-edits also auto-approves workspace edits; smart additionally auto-runs actions a fast model judges safe; dangerous auto-approves all. When omitted, Devin uses its own headless default; choose an explicit mode for unattended runs."
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy. legacy is supported. mcp_managed is rejected before Devin launches because the CLI adapter cannot isolate ambient MCP configuration; transport=acp has its own permission bridge and also rejects mcp_managed."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "On transport=cli, approvalPolicy has no effect because mcp_managed is unavailable. On transport=acp, supplying approvalPolicy is rejected because ACP has its own permission bridge."
          },
          "promptFile": {
            "type": "string",
            "description": "Load the initial prompt from a file (--prompt-file)"
          },
          "config": {
            "type": "string",
            "description": "Config file path (Devin --config <PATH>)"
          },
          "sandbox": {
            "type": "boolean",
            "description": "Run the Devin session in a sandbox (Devin --sandbox). Safety control: never defaulted on; pass true to opt in."
          },
          "exportSession": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "string"
              }
            ],
            "description": "Export the session (Devin --export [<PATH>]). true emits a bare --export; a string path emits --export <path>."
          },
          "respectWorkspaceTrust": {
            "type": "boolean",
            "description": "Respect workspace trust (Devin --respect-workspace-trust <bool>). Devin defaults true for interactive and false for print mode; set explicitly to override."
          },
          "agentConfig": {
            "type": "string",
            "description": "Agent config file path (Devin --agent-config <FILE>)"
          },
          "agentType": {
            "type": "string",
            "enum": [
              "summarizer",
              "review"
            ],
            "description": "ACP agent variant for transport=acp (`devin acp --agent-type`): 'summarizer' (no tools, text summary) or 'review' (read-only + shell code-review). Ignored for the CLI transport."
          },
          "sessionId": {
            "type": "string",
            "description": "On transport=cli, Devin session ID to resume (emits --resume <id>; use resumeLatest for --continue); gateway gw-* tracking ids are not CLI session ids. On transport=acp, gateway-owned ACP session ID returned by an earlier ACP call; Devin-native and CLI session ids are rejected."
          },
          "resumeLatest": {
            "type": "boolean",
            "default": false,
            "description": "Resume the most recent Devin session in cwd (--continue) on transport=cli. true is rejected on transport=acp."
          },
          "createNewSession": {
            "type": "boolean",
            "default": false,
            "description": "Force a new session on transport=cli. true is rejected on transport=acp."
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "optimizePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Optimize prompt before execution"
          },
          "optimizeResponse": {
            "type": "boolean",
            "default": false,
            "description": "Optimize response output"
          },
          "compressResponse": {
            "type": "boolean",
            "description": "Compress the response display text via the native compressor (default: [compression].enabled in config.toml, off unless opted in). Skipped for structured output."
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h, omit=CLI default)"
          },
          "forceRefresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass dedup and force a fresh CLI run even if a recent identical request exists"
          },
          "workingDir": {
            "type": "string",
            "minLength": 1,
            "description": "Local Devin process working directory. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Do not use this field, workspace_list, or workspace_register_existing_repo as a fallback for stdio/local provider path access; pass workingDir/addDir/includeDirs directly instead."
          },
          "worktree": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 64
                  },
                  "ref": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 255
                  }
                },
                "additionalProperties": false
              }
            ],
            "description": "Slice λ: run this request inside a dedicated git worktree owned by the gateway. `true` creates a fresh worktree at `<repoRoot>/.worktrees/<uuid>` branched from HEAD. `{ name?, ref? }` lets the caller supply a sanitized name and/or a git ref (default: HEAD). When the request carries a sessionId and the session already has a worktree, reuse requires same-host ownership metadata and a matching live Git registration. Named path collisions never reuse manager state. Gateway-managed worktrees require the local file-backed session manager and fail closed with PostgreSQL sessions. The Grok, Devin, and Mistral adapters require an explicit provider-native sessionId; fresh, createNewSession, and resumeLatest-only worktree requests fail closed because they cannot durably reselect the worktree. The request must select a registered workspace explicitly, through caller-owned session metadata, or through the configured default. Worktrees never fall back to the gateway process cwd and cannot be combined with local workingDir, addDir, or includeDirs paths. The gateway spawns the child CLI with `cwd: <worktree-path>`; no `-w`/`--worktree` flag is ever emitted to the underlying CLI. On worktree materialization, the gateway suppresses repository, system, and global Git hooks and configured clean, smudge, and process checkout filters, sparse checkout, and lazy object fetching. Filter-dependent content such as Git LFS remains in its repository representation instead of executing host commands. On session_delete or TTL eviction the gateway hides the session and runs `git worktree remove --force`. Failed removal retains a durable cleanup-pending tombstone which blocks reuse and is retried when the file store is registered on the owning host. The tombstone is finalized only after verified Git removal. Successful responses are prefixed with `[gateway] worktree=<absolute-path>\\n` so callers can use the path. For Claude approvalStrategy:mcp_managed, requesting or reusing a worktree requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1. Other adapters reject mcp_managed before launch. NOTE: callers should `.gitignore` the `.worktrees/` directory in their repo (the gateway does NOT auto-gitignore; see slice λ spec Q4)."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "devin_request_async",
      "description": "Start a Cognition Devin CLI request as a durable background job. Poll with llm_job_status, collect with llm_job_result.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for Devin CLI. Required in practice; promptFile is additive (loads an initial prompt from a file)."
          },
          "model": {
            "type": "string",
            "description": "Model name or alias (e.g. opus, latest)"
          },
          "permissionMode": {
            "type": "string",
            "enum": [
              "auto",
              "accept-edits",
              "smart",
              "dangerous"
            ],
            "description": "Devin CLI permission mode (--permission-mode). auto, accept-edits, smart, or dangerous. When omitted, Devin uses its own headless default; choose an explicit mode for unattended runs."
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy: legacy is supported. mcp_managed is rejected before launch because this adapter cannot isolate ambient MCP configuration."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "approvalPolicy is unavailable for this provider. It has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "promptFile": {
            "type": "string",
            "description": "Load the initial prompt from a file (--prompt-file)"
          },
          "config": {
            "type": "string",
            "description": "Config file path (Devin --config <PATH>)"
          },
          "sandbox": {
            "type": "boolean",
            "description": "Run the Devin session in a sandbox (Devin --sandbox). Safety control: never defaulted on; pass true to opt in."
          },
          "exportSession": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "string"
              }
            ],
            "description": "Export the session (Devin --export [<PATH>]). true emits a bare --export; a string path emits --export <path>."
          },
          "respectWorkspaceTrust": {
            "type": "boolean",
            "description": "Respect workspace trust (Devin --respect-workspace-trust <bool>). Devin defaults true for interactive and false for print mode; set explicitly to override."
          },
          "agentConfig": {
            "type": "string",
            "description": "Agent config file path (Devin --agent-config <FILE>)"
          },
          "sessionId": {
            "type": "string",
            "description": "Devin session ID to resume (--resume <id>; use resumeLatest for --continue). Note: the gw-* id minted for a brand-new session is not resumable via sessionId; continue with resumeLatest:true."
          },
          "resumeLatest": {
            "type": "boolean",
            "default": false,
            "description": "Resume the most recent Devin session in cwd (--continue). Note: the gw-* id minted for a brand-new session is not resumable via sessionId; continue with resumeLatest:true."
          },
          "createNewSession": {
            "type": "boolean",
            "default": false,
            "description": "Force a new session"
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "optimizePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Optimize prompt before execution"
          },
          "compressResponse": {
            "type": "boolean",
            "description": "Compress the response display text when collected via llm_job_result (native compressor; default: [compression].enabled)."
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h, omit=CLI default)"
          },
          "forceRefresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass dedup and force a fresh CLI run even if a recent identical request exists"
          },
          "workingDir": {
            "type": "string",
            "minLength": 1,
            "description": "Local Devin process working directory. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Do not use this field, workspace_list, or workspace_register_existing_repo as a fallback for stdio/local provider path access; pass workingDir/addDir/includeDirs directly instead."
          },
          "worktree": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 64
                  },
                  "ref": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 255
                  }
                },
                "additionalProperties": false
              }
            ],
            "description": "Slice λ: run this request inside a dedicated git worktree owned by the gateway. `true` creates a fresh worktree at `<repoRoot>/.worktrees/<uuid>` branched from HEAD. `{ name?, ref? }` lets the caller supply a sanitized name and/or a git ref (default: HEAD). When the request carries a sessionId and the session already has a worktree, reuse requires same-host ownership metadata and a matching live Git registration. Named path collisions never reuse manager state. Gateway-managed worktrees require the local file-backed session manager and fail closed with PostgreSQL sessions. The Grok, Devin, and Mistral adapters require an explicit provider-native sessionId; fresh, createNewSession, and resumeLatest-only worktree requests fail closed because they cannot durably reselect the worktree. The request must select a registered workspace explicitly, through caller-owned session metadata, or through the configured default. Worktrees never fall back to the gateway process cwd and cannot be combined with local workingDir, addDir, or includeDirs paths. The gateway spawns the child CLI with `cwd: <worktree-path>`; no `-w`/`--worktree` flag is ever emitted to the underlying CLI. On worktree materialization, the gateway suppresses repository, system, and global Git hooks and configured clean, smudge, and process checkout filters, sparse checkout, and lazy object fetching. Filter-dependent content such as Git LFS remains in its repository representation instead of executing host commands. On session_delete or TTL eviction the gateway hides the session and runs `git worktree remove --force`. Failed removal retains a durable cleanup-pending tombstone which blocks reuse and is retried when the file store is registered on the owning host. The tombstone is finalized only after verified Git removal. Successful responses are prefixed with `[gateway] worktree=<absolute-path>\\n` so callers can use the path. For Claude approvalStrategy:mcp_managed, requesting or reusing a worktree requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1. Other adapters reject mcp_managed before launch. NOTE: callers should `.gitignore` the `.worktrees/` directory in their repo (the gateway does NOT auto-gitignore; see slice λ spec Q4)."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "explain_effective_config",
      "description": "Explain the selected Personal Agent Config release and context provenance without returning local paths or instruction text.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "workingDir": {
            "type": "string",
            "minLength": 1,
            "description": "Absolute working directory whose Personal Agent Config Kit scope should be inspected. Relative paths are rejected so scope never depends on the gateway process cwd."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Do not use this field, workspace_list, or workspace_register_existing_repo as a fallback for stdio/local provider path access; pass workingDir/addDir/includeDirs directly instead."
          },
          "requestInstructions": {
            "type": "string",
            "maxLength": 16384
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "gemini_request",
      "description": "Run a Google Antigravity CLI (`agy`) request through the Gemini-compatible gateway tool synchronously (when async jobs are enabled, auto-defers to a pollable job past the sync deadline; otherwise runs to completion). Requires exactly one of prompt or promptParts.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for Antigravity CLI (mutually exclusive with promptParts)"
          },
          "promptParts": {
            "type": "object",
            "properties": {
              "system": {
                "type": "string",
                "maxLength": 100000
              },
              "tools": {
                "type": "string",
                "maxLength": 100000
              },
              "context": {
                "type": "string",
                "maxLength": 100000
              },
              "task": {
                "type": "string",
                "minLength": 1,
                "maxLength": 100000
              },
              "cacheControl": {
                "type": "object",
                "properties": {
                  "system": {
                    "type": "boolean"
                  },
                  "tools": {
                    "type": "boolean"
                  },
                  "context": {
                    "type": "boolean"
                  }
                },
                "additionalProperties": false
              }
            },
            "required": [
              "task"
            ],
            "additionalProperties": false,
            "description": "Cache-aware structured prompt: { system?, tools?, context?, task }. Mutually exclusive with prompt. Stable parts hash into cache_state for prefix-discipline tracking."
          },
          "model": {
            "type": "string",
            "description": "Model name or alias passed to agy --model (e.g. gemini-3-pro-preview, gemini-2.5-flash, pro, flash, latest)"
          },
          "sessionId": {
            "type": "string",
            "minLength": 1,
            "description": "Antigravity conversation ID to resume (emits --conversation <id>). agy owns conversation ids; a fresh request returns no resumable sessionId, continue via resumeLatest:true."
          },
          "resumeLatest": {
            "type": "boolean",
            "default": false,
            "description": "Continue the most recent conversation. agy owns conversation ids; a fresh request returns no resumable sessionId, continue via resumeLatest:true."
          },
          "createNewSession": {
            "type": "boolean",
            "default": false,
            "description": "Force new session"
          },
          "approvalMode": {
            "type": "string",
            "enum": [
              "default",
              "auto_edit",
              "yolo",
              "plan"
            ],
            "description": "Approval mode for legacy strategy: default leaves agy prompted, auto_edit emits --mode accept-edits, plan emits --mode plan, and yolo emits --dangerously-skip-permissions."
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy: legacy is supported. mcp_managed is rejected before launch because this adapter cannot isolate ambient MCP configuration."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "approvalPolicy is unavailable for this provider. It has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "mcpServers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "description": "This provider does not receive gateway-managed MCP configuration. mcpServers has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "allowedTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Unsupported for Antigravity CLI; non-empty values are rejected"
          },
          "includeDirs": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Additional workspace directories passed as --add-dir. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "optimizePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Optimize prompt before execution"
          },
          "optimizeResponse": {
            "type": "boolean",
            "default": false,
            "description": "Optimize response output"
          },
          "compressResponse": {
            "type": "boolean",
            "description": "Compress the response display text via the native compressor (default: [compression].enabled in config.toml, off unless opted in). Skipped for structured output."
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h, omit=CLI default)"
          },
          "forceRefresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass dedup and force a fresh CLI run even if a recent identical request exists"
          },
          "outputFormat": {
            "type": "string",
            "enum": [
              "text",
              "json",
              "stream-json"
            ],
            "default": "text",
            "description": "Output format. The Antigravity agy headless path emits text only; json and stream-json are rejected at request time. Per-request token usage and cost are therefore not available for gemini."
          },
          "sandbox": {
            "type": "boolean",
            "description": "Run Antigravity in sandbox mode (--sandbox)"
          },
          "policyFiles": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Unsupported for Antigravity CLI; non-empty values are rejected."
          },
          "adminPolicyFiles": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Unsupported for Antigravity CLI; non-empty values are rejected."
          },
          "attachments": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Unsupported for Antigravity CLI; non-empty values are rejected."
          },
          "skipTrust": {
            "type": "boolean",
            "default": false,
            "description": "Unsupported for Antigravity CLI; true is rejected."
          },
          "yolo": {
            "type": "boolean",
            "description": "Emit `--dangerously-skip-permissions` to auto-approve all actions."
          },
          "project": {
            "type": "string",
            "minLength": 1,
            "description": "Antigravity 1.0.13 --project <ID>: select the project for this session. Mutually exclusive with newProject."
          },
          "newProject": {
            "type": "boolean",
            "description": "Antigravity 1.0.13 --new-project: create a new project for this session. Mutually exclusive with project."
          },
          "printTimeout": {
            "type": "string",
            "minLength": 1,
            "description": "Antigravity --print-timeout <DURATION>: print-mode wait timeout as a Go duration string (e.g. '5m0s', '30s')."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Do not use this field, workspace_list, or workspace_register_existing_repo as a fallback for stdio/local provider path access; pass workingDir/addDir/includeDirs directly instead."
          },
          "worktree": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 64
                  },
                  "ref": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 255
                  }
                },
                "additionalProperties": false
              }
            ],
            "description": "Slice λ: run this request inside a dedicated git worktree owned by the gateway. `true` creates a fresh worktree at `<repoRoot>/.worktrees/<uuid>` branched from HEAD. `{ name?, ref? }` lets the caller supply a sanitized name and/or a git ref (default: HEAD). When the request carries a sessionId and the session already has a worktree, reuse requires same-host ownership metadata and a matching live Git registration. Named path collisions never reuse manager state. Gateway-managed worktrees require the local file-backed session manager and fail closed with PostgreSQL sessions. The Grok, Devin, and Mistral adapters require an explicit provider-native sessionId; fresh, createNewSession, and resumeLatest-only worktree requests fail closed because they cannot durably reselect the worktree. The request must select a registered workspace explicitly, through caller-owned session metadata, or through the configured default. Worktrees never fall back to the gateway process cwd and cannot be combined with local workingDir, addDir, or includeDirs paths. The gateway spawns the child CLI with `cwd: <worktree-path>`; no `-w`/`--worktree` flag is ever emitted to the underlying CLI. On worktree materialization, the gateway suppresses repository, system, and global Git hooks and configured clean, smudge, and process checkout filters, sparse checkout, and lazy object fetching. Filter-dependent content such as Git LFS remains in its repository representation instead of executing host commands. On session_delete or TTL eviction the gateway hides the session and runs `git worktree remove --force`. Failed removal retains a durable cleanup-pending tombstone which blocks reuse and is retried when the file store is registered on the owning host. The tombstone is finalized only after verified Git removal. Successful responses are prefixed with `[gateway] worktree=<absolute-path>\\n` so callers can use the path. For Claude approvalStrategy:mcp_managed, requesting or reusing a worktree requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1. Other adapters reject mcp_managed before launch. NOTE: callers should `.gitignore` the `.worktrees/` directory in their repo (the gateway does NOT auto-gitignore; see slice λ spec Q4)."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "gemini_request_async",
      "description": "Start a Google Antigravity CLI (`agy`) request as a durable background job through the Gemini-compatible gateway tool. Poll with llm_job_status, collect with llm_job_result.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for Antigravity CLI (mutually exclusive with promptParts)"
          },
          "promptParts": {
            "type": "object",
            "properties": {
              "system": {
                "type": "string",
                "maxLength": 100000
              },
              "tools": {
                "type": "string",
                "maxLength": 100000
              },
              "context": {
                "type": "string",
                "maxLength": 100000
              },
              "task": {
                "type": "string",
                "minLength": 1,
                "maxLength": 100000
              },
              "cacheControl": {
                "type": "object",
                "properties": {
                  "system": {
                    "type": "boolean"
                  },
                  "tools": {
                    "type": "boolean"
                  },
                  "context": {
                    "type": "boolean"
                  }
                },
                "additionalProperties": false
              }
            },
            "required": [
              "task"
            ],
            "additionalProperties": false,
            "description": "Cache-aware structured prompt: { system?, tools?, context?, task }. Mutually exclusive with prompt. Stable parts hash into cache_state for prefix-discipline tracking."
          },
          "model": {
            "type": "string",
            "description": "Model name or alias passed to agy --model (e.g. gemini-3-pro-preview, gemini-2.5-flash, pro, flash, latest)"
          },
          "sessionId": {
            "type": "string",
            "minLength": 1,
            "description": "Antigravity conversation ID to resume (emits --conversation <id>). agy owns conversation ids; a fresh request returns no resumable sessionId, continue via resumeLatest:true."
          },
          "resumeLatest": {
            "type": "boolean",
            "default": false,
            "description": "Continue the most recent conversation. agy owns conversation ids; a fresh request returns no resumable sessionId, continue via resumeLatest:true."
          },
          "createNewSession": {
            "type": "boolean",
            "default": false,
            "description": "Force new session"
          },
          "approvalMode": {
            "type": "string",
            "enum": [
              "default",
              "auto_edit",
              "yolo",
              "plan"
            ],
            "description": "Approval mode for legacy strategy: default leaves agy prompted, auto_edit emits --mode accept-edits, plan emits --mode plan, and yolo emits --dangerously-skip-permissions."
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy: legacy is supported. mcp_managed is rejected before launch because this adapter cannot isolate ambient MCP configuration."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "approvalPolicy is unavailable for this provider. It has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "mcpServers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "description": "This provider does not receive gateway-managed MCP configuration. mcpServers has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "allowedTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Unsupported for Antigravity CLI; non-empty values are rejected"
          },
          "includeDirs": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Additional workspace directories passed as --add-dir. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "optimizePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Optimize prompt before execution"
          },
          "compressResponse": {
            "type": "boolean",
            "description": "Compress the response display text when collected via llm_job_result (native compressor; default: [compression].enabled)."
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h, omit=CLI default)"
          },
          "forceRefresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass dedup and force a fresh CLI run even if a recent identical request exists"
          },
          "outputFormat": {
            "type": "string",
            "enum": [
              "text",
              "json",
              "stream-json"
            ],
            "default": "text",
            "description": "Output format. The Antigravity agy headless path emits text only; json and stream-json are rejected at request time. Per-request token usage and cost are therefore not available for gemini."
          },
          "sandbox": {
            "type": "boolean",
            "description": "Run Antigravity in sandbox mode (--sandbox)"
          },
          "policyFiles": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Unsupported for Antigravity CLI; non-empty values are rejected."
          },
          "adminPolicyFiles": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Unsupported for Antigravity CLI; non-empty values are rejected."
          },
          "attachments": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Unsupported for Antigravity CLI; non-empty values are rejected."
          },
          "skipTrust": {
            "type": "boolean",
            "default": false,
            "description": "Unsupported for Antigravity CLI; true is rejected."
          },
          "yolo": {
            "type": "boolean",
            "description": "Emit `--dangerously-skip-permissions` to auto-approve all actions."
          },
          "project": {
            "type": "string",
            "minLength": 1,
            "description": "Antigravity 1.0.13 --project <ID>: select the project for this session. Mutually exclusive with newProject."
          },
          "newProject": {
            "type": "boolean",
            "description": "Antigravity 1.0.13 --new-project: create a new project for this session. Mutually exclusive with project."
          },
          "printTimeout": {
            "type": "string",
            "minLength": 1,
            "description": "Antigravity --print-timeout <DURATION>: print-mode wait timeout as a Go duration string (e.g. '5m0s', '30s')."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Do not use this field, workspace_list, or workspace_register_existing_repo as a fallback for stdio/local provider path access; pass workingDir/addDir/includeDirs directly instead."
          },
          "worktree": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 64
                  },
                  "ref": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 255
                  }
                },
                "additionalProperties": false
              }
            ],
            "description": "Slice λ: run this request inside a dedicated git worktree owned by the gateway. `true` creates a fresh worktree at `<repoRoot>/.worktrees/<uuid>` branched from HEAD. `{ name?, ref? }` lets the caller supply a sanitized name and/or a git ref (default: HEAD). When the request carries a sessionId and the session already has a worktree, reuse requires same-host ownership metadata and a matching live Git registration. Named path collisions never reuse manager state. Gateway-managed worktrees require the local file-backed session manager and fail closed with PostgreSQL sessions. The Grok, Devin, and Mistral adapters require an explicit provider-native sessionId; fresh, createNewSession, and resumeLatest-only worktree requests fail closed because they cannot durably reselect the worktree. The request must select a registered workspace explicitly, through caller-owned session metadata, or through the configured default. Worktrees never fall back to the gateway process cwd and cannot be combined with local workingDir, addDir, or includeDirs paths. The gateway spawns the child CLI with `cwd: <worktree-path>`; no `-w`/`--worktree` flag is ever emitted to the underlying CLI. On worktree materialization, the gateway suppresses repository, system, and global Git hooks and configured clean, smudge, and process checkout filters, sparse checkout, and lazy object fetching. Filter-dependent content such as Git LFS remains in its repository representation instead of executing host commands. On session_delete or TTL eviction the gateway hides the session and runs `git worktree remove --force`. Failed removal retains a durable cleanup-pending tombstone which blocks reuse and is retried when the file store is registered on the owning host. The tombstone is finalized only after verified Git removal. Successful responses are prefixed with `[gateway] worktree=<absolute-path>\\n` so callers can use the path. For Claude approvalStrategy:mcp_managed, requesting or reusing a worktree requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1. Other adapters reject mcp_managed before launch. NOTE: callers should `.gitignore` the `.worktrees/` directory in their repo (the gateway does NOT auto-gitignore; see slice λ spec Q4)."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "grok_request",
      "description": "Run an xAI Grok CLI request synchronously (when async jobs are enabled, auto-defers to a pollable job past the sync deadline; otherwise runs to completion). Requires exactly one of prompt or promptParts.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for Grok (mutually exclusive with promptParts)"
          },
          "promptParts": {
            "type": "object",
            "properties": {
              "system": {
                "type": "string",
                "maxLength": 100000
              },
              "tools": {
                "type": "string",
                "maxLength": 100000
              },
              "context": {
                "type": "string",
                "maxLength": 100000
              },
              "task": {
                "type": "string",
                "minLength": 1,
                "maxLength": 100000
              },
              "cacheControl": {
                "type": "object",
                "properties": {
                  "system": {
                    "type": "boolean"
                  },
                  "tools": {
                    "type": "boolean"
                  },
                  "context": {
                    "type": "boolean"
                  }
                },
                "additionalProperties": false
              }
            },
            "required": [
              "task"
            ],
            "additionalProperties": false,
            "description": "Cache-aware structured prompt: { system?, tools?, context?, task }. Mutually exclusive with prompt. Stable parts hash into cache_state for prefix-discipline tracking."
          },
          "model": {
            "type": "string",
            "description": "Model name or alias (e.g. grok-build, latest)"
          },
          "transport": {
            "type": "string",
            "enum": [
              "cli",
              "acp"
            ],
            "default": "cli",
            "description": "Transport: 'cli' (default) runs the Grok CLI. 'acp' routes through `grok agent stdio` only when [acp].enabled plus [acp.providers.grok].enabled and runtime_enabled are true (fails closed otherwise). ACP accepts prompt, model, a gateway ACP sessionId, and a registered workspace alias; Grok CLI-only controls are rejected."
          },
          "outputFormat": {
            "type": "string",
            "enum": [
              "plain",
              "json",
              "streaming-json"
            ],
            "description": "Output format (plain|json|streaming-json). Grok default is plain."
          },
          "effort": {
            "type": "string",
            "enum": [
              "low",
              "medium",
              "high",
              "xhigh",
              "max"
            ],
            "description": "Grok effort level"
          },
          "reasoningEffort": {
            "type": "string",
            "description": "Reasoning effort for reasoning models"
          },
          "allowedTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Allowed built-in tools (passed as --tools comma list)"
          },
          "disallowedTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Disallowed built-in tools (passed as --disallowed-tools comma list)"
          },
          "maxTurns": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "minimum": -9007199254740991,
            "maximum": 10000,
            "description": "Grok `--max-turns N`: cap on agent-loop iterations for cost / latency control (Phase 4 slice δ). Bounded to safe integers ≤ 10000."
          },
          "workingDir": {
            "type": "string",
            "minLength": 1,
            "description": "Grok --cwd <DIR>: working directory for this invocation. Lets headless callers run Grok against a directory other than the gateway process's cwd."
          },
          "sandbox": {
            "type": "string",
            "minLength": 1,
            "description": "Grok --sandbox <PROFILE>: sandbox profile for filesystem and network access. Freeform per `grok --help` (no enum constraint on Grok 0.1.210); also settable via GROK_SANDBOX env var. Caller responsibility to pass a valid profile name."
          },
          "rules": {
            "type": "string",
            "minLength": 1,
            "description": "Grok --rules <RULES>: extra rules to append to the system prompt. Supports `@file` prefix per `grok --help` to load from a file; gateway passes the value verbatim and lets Grok parse the prefix."
          },
          "systemPromptOverride": {
            "type": "string",
            "minLength": 1,
            "description": "Grok --system-prompt-override <PROMPT>: replace the agent's system prompt entirely. Distinct from Claude's --system-prompt / --append-system-prompt (Grok has only one override flag, not a pair)."
          },
          "allow": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Grok --allow <RULE>: permission allow rules. Each entry is emitted as its own --allow instance (per `grok --help`: \"Repeat to add multiple rules\")."
          },
          "deny": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Grok --deny <RULE>: permission deny rules. Each entry is emitted as its own --deny instance (per `grok --help`: \"Repeat to add multiple rules\")."
          },
          "compactionMode": {
            "type": "string",
            "enum": [
              "summary",
              "transcript",
              "segments"
            ],
            "description": "Grok --compaction-mode: summary (default; no pointer) | transcript (points at the raw transcript) | segments (persists per-segment markdown to grep). Sets GROK_COMPACTION_MODE."
          },
          "compactionDetail": {
            "type": "string",
            "enum": [
              "none",
              "minimal",
              "balanced",
              "verbose"
            ],
            "description": "Grok --compaction-detail: verbatim segment detail (none|minimal|balanced|verbose, default verbose). Only affects `--compaction-mode segments`. Sets GROK_COMPACTION_DETAIL."
          },
          "agent": {
            "type": "string",
            "minLength": 1,
            "description": "Grok --agent <NAME>: agent name or definition file path."
          },
          "bestOfN": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "minimum": -9007199254740991,
            "maximum": 10000,
            "description": "Grok --best-of-n <N>: run the task N ways in parallel and pick the best (headless only)."
          },
          "check": {
            "type": "boolean",
            "description": "Grok --check: append a self-verification loop to the prompt (headless only)."
          },
          "disableWebSearch": {
            "type": "boolean",
            "description": "Grok --disable-web-search: disable web search and remote retrieval tools."
          },
          "todoGate": {
            "type": "boolean",
            "description": "Grok --todo-gate: enable runtime turn-end TodoGate for this session (session-scoped, not persisted)."
          },
          "verbatim": {
            "type": "boolean",
            "description": "Grok --verbatim: send the prompt exactly as given. Also skips gateway optimizePrompt when true."
          },
          "promptFile": {
            "type": "string",
            "minLength": 1,
            "description": "Grok --prompt-file <PATH>: single-turn prompt loaded from a file."
          },
          "experimentalMemory": {
            "type": "boolean",
            "description": "Grok --experimental-memory: enable cross-session memory."
          },
          "noAltScreen": {
            "type": "boolean",
            "description": "Grok --no-alt-screen: run inline without alt screen."
          },
          "noMemory": {
            "type": "boolean",
            "description": "Grok --no-memory: disable cross-session memory."
          },
          "noPlan": {
            "type": "boolean",
            "description": "Grok --no-plan: disable plan mode."
          },
          "noSubagents": {
            "type": "boolean",
            "description": "Grok --no-subagents: disable subagent spawning."
          },
          "oauth": {
            "type": "boolean",
            "description": "Grok --oauth: use OAuth during authentication."
          },
          "restoreCode": {
            "type": "boolean",
            "description": "Grok --restore-code: check out the original session commit when resuming."
          },
          "leaderSocket": {
            "type": "string",
            "minLength": 1,
            "description": "Grok 0.2.32+ --leader-socket <PATH>: custom leader socket path (default ~/.grok/leader.sock). Targets an isolated leader process, e.g. a local/branch Grok build; name it ~/.grok/leader-*.sock to keep `grok leader list/kill` discovery working."
          },
          "sessionId": {
            "type": "string",
            "description": "On transport=cli, provider-native session ID to resume (emits --resume <id>; use resumeLatest for --continue); gateway gw-* tracking ids are not CLI session ids. On transport=acp, gateway-owned ACP session ID returned by an earlier ACP call; provider-native and CLI session ids are rejected."
          },
          "resumeLatest": {
            "type": "boolean",
            "default": false,
            "description": "Resume the most recent Grok session in cwd (--continue) on transport=cli. true is rejected on transport=acp."
          },
          "createNewSession": {
            "type": "boolean",
            "default": false,
            "description": "Force a new session on transport=cli. true is rejected on transport=acp."
          },
          "alwaysApprove": {
            "type": "boolean",
            "default": false,
            "description": "Auto-approve all tool executions (--always-approve)."
          },
          "permissionMode": {
            "type": "string",
            "enum": [
              "default",
              "acceptEdits",
              "auto",
              "dontAsk",
              "bypassPermissions",
              "plan"
            ],
            "description": "Grok permission mode: default|acceptEdits|auto|dontAsk|bypassPermissions|plan."
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy. legacy is supported. mcp_managed is rejected before Grok launches because the CLI adapter cannot isolate ambient MCP configuration; transport=acp has its own permission bridge and also rejects mcp_managed."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "On transport=cli, approvalPolicy has no effect because mcp_managed is unavailable. On transport=acp, supplying approvalPolicy is rejected because ACP has its own permission bridge."
          },
          "mcpServers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "description": "This provider does not receive gateway-managed MCP configuration. mcpServers has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "optimizePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Optimize prompt before execution"
          },
          "optimizeResponse": {
            "type": "boolean",
            "default": false,
            "description": "Optimize response output"
          },
          "compressResponse": {
            "type": "boolean",
            "description": "Compress the response display text via the native compressor (default: [compression].enabled in config.toml, off unless opted in). Skipped for structured output."
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h). Omit = gateway default of 600000ms (10 min) with no output before the process is killed."
          },
          "forceRefresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass dedup and force a fresh CLI run even if a recent identical request exists"
          },
          "agents": {
            "anyOf": [
              {
                "type": "string",
                "minLength": 1
              },
              {
                "type": "object",
                "additionalProperties": {}
              }
            ],
            "description": "Grok --agents <JSON>: inline subagent definitions (JSON string or name → { description, prompt, … } map)."
          },
          "promptJson": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "array",
                "items": {}
              },
              {
                "type": "object",
                "additionalProperties": {}
              }
            ],
            "description": "Grok --prompt-json <JSON>: single-turn prompt JSON blocks (string or serializable value)."
          },
          "nativeWorktree": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "string",
                "minLength": 1
              }
            ],
            "description": "Grok -w/--worktree: native CLI worktree flag (`true` → bare `--worktree`, string → named). NOT gateway slice λ `worktree`."
          },
          "worktreeRef": {
            "type": "string",
            "minLength": 1,
            "description": "Grok 0.2.73 --worktree-ref <REF>: git ref (branch/tag/commit) to base the native worktree on. Requires nativeWorktree."
          },
          "forkSession": {
            "type": "boolean",
            "description": "Grok 0.2.73 --fork-session: when resuming (--resume/--continue), fork into a new session ID instead of reusing the original."
          },
          "jsonSchema": {
            "anyOf": [
              {
                "type": "string",
                "minLength": 1
              },
              {
                "type": "object",
                "additionalProperties": {}
              }
            ],
            "description": "Grok 0.2.73 --json-schema: constrain output to a JSON Schema (string literal or object; implies json output). Set outputFormat:json so the gateway treats the reply as structured output (skips response optimization and warning injection); the default output format is not json, so pass it explicitly."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Do not use this field, workspace_list, or workspace_register_existing_repo as a fallback for stdio/local provider path access; pass workingDir/addDir/includeDirs directly instead."
          },
          "worktree": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 64
                  },
                  "ref": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 255
                  }
                },
                "additionalProperties": false
              }
            ],
            "description": "Slice λ: run this request inside a dedicated git worktree owned by the gateway. `true` creates a fresh worktree at `<repoRoot>/.worktrees/<uuid>` branched from HEAD. `{ name?, ref? }` lets the caller supply a sanitized name and/or a git ref (default: HEAD). When the request carries a sessionId and the session already has a worktree, reuse requires same-host ownership metadata and a matching live Git registration. Named path collisions never reuse manager state. Gateway-managed worktrees require the local file-backed session manager and fail closed with PostgreSQL sessions. The Grok, Devin, and Mistral adapters require an explicit provider-native sessionId; fresh, createNewSession, and resumeLatest-only worktree requests fail closed because they cannot durably reselect the worktree. The request must select a registered workspace explicitly, through caller-owned session metadata, or through the configured default. Worktrees never fall back to the gateway process cwd and cannot be combined with local workingDir, addDir, or includeDirs paths. The gateway spawns the child CLI with `cwd: <worktree-path>`; no `-w`/`--worktree` flag is ever emitted to the underlying CLI. On worktree materialization, the gateway suppresses repository, system, and global Git hooks and configured clean, smudge, and process checkout filters, sparse checkout, and lazy object fetching. Filter-dependent content such as Git LFS remains in its repository representation instead of executing host commands. On session_delete or TTL eviction the gateway hides the session and runs `git worktree remove --force`. Failed removal retains a durable cleanup-pending tombstone which blocks reuse and is retried when the file store is registered on the owning host. The tombstone is finalized only after verified Git removal. Successful responses are prefixed with `[gateway] worktree=<absolute-path>\\n` so callers can use the path. For Claude approvalStrategy:mcp_managed, requesting or reusing a worktree requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1. Other adapters reject mcp_managed before launch. NOTE: callers should `.gitignore` the `.worktrees/` directory in their repo (the gateway does NOT auto-gitignore; see slice λ spec Q4)."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "grok_request_async",
      "description": "Start an xAI Grok CLI request as a durable background job. Poll with llm_job_status, collect with llm_job_result.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for Grok (mutually exclusive with promptParts)"
          },
          "promptParts": {
            "type": "object",
            "properties": {
              "system": {
                "type": "string",
                "maxLength": 100000
              },
              "tools": {
                "type": "string",
                "maxLength": 100000
              },
              "context": {
                "type": "string",
                "maxLength": 100000
              },
              "task": {
                "type": "string",
                "minLength": 1,
                "maxLength": 100000
              },
              "cacheControl": {
                "type": "object",
                "properties": {
                  "system": {
                    "type": "boolean"
                  },
                  "tools": {
                    "type": "boolean"
                  },
                  "context": {
                    "type": "boolean"
                  }
                },
                "additionalProperties": false
              }
            },
            "required": [
              "task"
            ],
            "additionalProperties": false,
            "description": "Cache-aware structured prompt: { system?, tools?, context?, task }. Mutually exclusive with prompt. Stable parts hash into cache_state for prefix-discipline tracking."
          },
          "model": {
            "type": "string",
            "description": "Model name or alias (e.g. grok-build, latest)"
          },
          "outputFormat": {
            "type": "string",
            "enum": [
              "plain",
              "json",
              "streaming-json"
            ],
            "description": "Output format (plain|json|streaming-json). Grok default is plain."
          },
          "sessionId": {
            "type": "string",
            "description": "Provider-native session ID to resume (emits --resume <id>; use resumeLatest for --continue). Note: the gw-* id minted for a brand-new session is not resumable via sessionId; continue with resumeLatest:true."
          },
          "resumeLatest": {
            "type": "boolean",
            "default": false,
            "description": "Resume most recent Grok session in cwd (--continue). Note: the gw-* id minted for a brand-new session is not resumable via sessionId; continue with resumeLatest:true."
          },
          "createNewSession": {
            "type": "boolean",
            "default": false,
            "description": "Force new session"
          },
          "alwaysApprove": {
            "type": "boolean",
            "default": false,
            "description": "Auto-approve all tool executions (--always-approve)."
          },
          "permissionMode": {
            "type": "string",
            "enum": [
              "default",
              "acceptEdits",
              "auto",
              "dontAsk",
              "bypassPermissions",
              "plan"
            ],
            "description": "Grok permission mode: default|acceptEdits|auto|dontAsk|bypassPermissions|plan."
          },
          "effort": {
            "type": "string",
            "enum": [
              "low",
              "medium",
              "high",
              "xhigh",
              "max"
            ],
            "description": "Grok effort level"
          },
          "reasoningEffort": {
            "type": "string",
            "description": "Reasoning effort for reasoning models"
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy: legacy is supported. mcp_managed is rejected before launch because this adapter cannot isolate ambient MCP configuration."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "approvalPolicy is unavailable for this provider. It has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "mcpServers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "description": "This provider does not receive gateway-managed MCP configuration. mcpServers has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "allowedTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Allowed built-in tools (passed as --tools comma list)"
          },
          "disallowedTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Disallowed built-in tools (passed as --disallowed-tools comma list)"
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "optimizePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Optimize prompt before execution"
          },
          "compressResponse": {
            "type": "boolean",
            "description": "Compress the response display text when collected via llm_job_result (native compressor; default: [compression].enabled)."
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h). Omit = gateway default of 600000ms (10 min) with no output before the process is killed."
          },
          "forceRefresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass dedup and force a fresh CLI run even if a recent identical request exists"
          },
          "maxTurns": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "minimum": -9007199254740991,
            "maximum": 10000,
            "description": "Grok `--max-turns N`: cap on agent-loop iterations for cost / latency control (Phase 4 slice δ). Bounded to safe integers ≤ 10000."
          },
          "workingDir": {
            "type": "string",
            "minLength": 1,
            "description": "Grok --cwd <DIR>: working directory for this invocation. Lets headless callers run Grok against a directory other than the gateway process's cwd. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "sandbox": {
            "type": "string",
            "minLength": 1,
            "description": "Grok --sandbox <PROFILE>: sandbox profile for filesystem and network access. Freeform per `grok --help` (no enum constraint); also settable via GROK_SANDBOX env var."
          },
          "rules": {
            "type": "string",
            "minLength": 1,
            "description": "Grok --rules <RULES>: extra rules to append to the system prompt. Supports `@file` prefix; gateway passes the value verbatim."
          },
          "systemPromptOverride": {
            "type": "string",
            "minLength": 1,
            "description": "Grok --system-prompt-override <PROMPT>: replace the agent's system prompt entirely."
          },
          "allow": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Grok --allow <RULE>: permission allow rules. Each entry → its own --allow instance."
          },
          "deny": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Grok --deny <RULE>: permission deny rules. Each entry → its own --deny instance."
          },
          "compactionMode": {
            "type": "string",
            "enum": [
              "summary",
              "transcript",
              "segments"
            ],
            "description": "Grok --compaction-mode: summary (default) | transcript | segments. Sets GROK_COMPACTION_MODE."
          },
          "compactionDetail": {
            "type": "string",
            "enum": [
              "none",
              "minimal",
              "balanced",
              "verbose"
            ],
            "description": "Grok --compaction-detail: segment verbatim detail (none|minimal|balanced|verbose, default verbose). Only affects segments mode. Sets GROK_COMPACTION_DETAIL."
          },
          "agent": {
            "type": "string",
            "minLength": 1,
            "description": "Grok --agent <NAME>: agent name or definition file path."
          },
          "bestOfN": {
            "$ref": "#/properties/maxTurns",
            "description": "Grok --best-of-n <N>: run the task N ways in parallel and pick the best (headless only)."
          },
          "check": {
            "type": "boolean",
            "description": "Grok --check: append a self-verification loop to the prompt (headless only)."
          },
          "disableWebSearch": {
            "type": "boolean",
            "description": "Grok --disable-web-search: disable web search and remote retrieval tools."
          },
          "todoGate": {
            "type": "boolean",
            "description": "Grok --todo-gate: enable runtime turn-end TodoGate for this session (session-scoped, not persisted)."
          },
          "verbatim": {
            "type": "boolean",
            "description": "Grok --verbatim: send the prompt exactly as given. Also skips gateway optimizePrompt when true."
          },
          "agents": {
            "anyOf": [
              {
                "type": "string",
                "minLength": 1
              },
              {
                "type": "object",
                "additionalProperties": {}
              }
            ],
            "description": "Grok --agents <JSON>: inline subagent definitions (JSON string or name → { description, prompt, … } map)."
          },
          "promptFile": {
            "type": "string",
            "minLength": 1,
            "description": "Grok --prompt-file <PATH>: single-turn prompt loaded from a file."
          },
          "promptJson": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "array",
                "items": {}
              },
              {
                "type": "object",
                "additionalProperties": {}
              }
            ],
            "description": "Grok --prompt-json <JSON>: single-turn prompt JSON blocks (string or serializable value)."
          },
          "experimentalMemory": {
            "type": "boolean",
            "description": "Grok --experimental-memory: enable cross-session memory."
          },
          "noAltScreen": {
            "type": "boolean",
            "description": "Grok --no-alt-screen: run inline without alt screen."
          },
          "noMemory": {
            "type": "boolean",
            "description": "Grok --no-memory: disable cross-session memory."
          },
          "noPlan": {
            "type": "boolean",
            "description": "Grok --no-plan: disable plan mode."
          },
          "noSubagents": {
            "type": "boolean",
            "description": "Grok --no-subagents: disable subagent spawning."
          },
          "oauth": {
            "type": "boolean",
            "description": "Grok --oauth: use OAuth during authentication."
          },
          "restoreCode": {
            "type": "boolean",
            "description": "Grok --restore-code: check out the original session commit when resuming."
          },
          "leaderSocket": {
            "type": "string",
            "minLength": 1,
            "description": "Grok 0.2.32+ --leader-socket <PATH>: custom leader socket path (default ~/.grok/leader.sock). Targets an isolated leader process, e.g. a local/branch Grok build; name it ~/.grok/leader-*.sock to keep `grok leader list/kill` discovery working."
          },
          "nativeWorktree": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "string",
                "minLength": 1
              }
            ],
            "description": "Grok -w/--worktree: native CLI worktree flag (`true` → bare `--worktree`, string → named). NOT gateway slice λ `worktree`."
          },
          "worktreeRef": {
            "type": "string",
            "minLength": 1,
            "description": "Grok 0.2.73 --worktree-ref <REF>: git ref (branch/tag/commit) to base the native worktree on. Requires nativeWorktree."
          },
          "forkSession": {
            "type": "boolean",
            "description": "Grok 0.2.73 --fork-session: when resuming (--resume/--continue), fork into a new session ID instead of reusing the original."
          },
          "jsonSchema": {
            "anyOf": [
              {
                "type": "string",
                "minLength": 1
              },
              {
                "type": "object",
                "additionalProperties": {}
              }
            ],
            "description": "Grok 0.2.73 --json-schema: constrain output to a JSON Schema (string literal or object; implies json output). Set outputFormat:json so the gateway treats the reply as structured output (skips response optimization and warning injection); the default output format is not json, so pass it explicitly."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Do not use this field, workspace_list, or workspace_register_existing_repo as a fallback for stdio/local provider path access; pass workingDir/addDir/includeDirs directly instead."
          },
          "worktree": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 64
                  },
                  "ref": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 255
                  }
                },
                "additionalProperties": false
              }
            ],
            "description": "Slice λ: run this request inside a dedicated git worktree owned by the gateway. `true` creates a fresh worktree at `<repoRoot>/.worktrees/<uuid>` branched from HEAD. `{ name?, ref? }` lets the caller supply a sanitized name and/or a git ref (default: HEAD). When the request carries a sessionId and the session already has a worktree, reuse requires same-host ownership metadata and a matching live Git registration. Named path collisions never reuse manager state. Gateway-managed worktrees require the local file-backed session manager and fail closed with PostgreSQL sessions. The Grok, Devin, and Mistral adapters require an explicit provider-native sessionId; fresh, createNewSession, and resumeLatest-only worktree requests fail closed because they cannot durably reselect the worktree. The request must select a registered workspace explicitly, through caller-owned session metadata, or through the configured default. Worktrees never fall back to the gateway process cwd and cannot be combined with local workingDir, addDir, or includeDirs paths. The gateway spawns the child CLI with `cwd: <worktree-path>`; no `-w`/`--worktree` flag is ever emitted to the underlying CLI. On worktree materialization, the gateway suppresses repository, system, and global Git hooks and configured clean, smudge, and process checkout filters, sparse checkout, and lazy object fetching. Filter-dependent content such as Git LFS remains in its repository representation instead of executing host commands. On session_delete or TTL eviction the gateway hides the session and runs `git worktree remove --force`. Failed removal retains a durable cleanup-pending tombstone which blocks reuse and is retried when the file store is registered on the owning host. The tombstone is finalized only after verified Git removal. Successful responses are prefixed with `[gateway] worktree=<absolute-path>\\n` so callers can use the path. For Claude approvalStrategy:mcp_managed, requesting or reusing a worktree requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1. Other adapters reject mcp_managed before launch. NOTE: callers should `.gitignore` the `.worktrees/` directory in their repo (the gateway does NOT auto-gitignore; see slice λ spec Q4)."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "job_result",
      "description": "Collect a VALIDATION job's normalized provider output — distinct from llm_job_result, which returns raw provider request job output.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "jobId": {
            "type": "string",
            "minLength": 1,
            "description": "Validation job ID."
          },
          "provider": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor"
            ],
            "description": "Provider that produced the job, used for normalized validation output."
          },
          "maxChars": {
            "type": "integer",
            "minimum": 1000,
            "maximum": 2000000,
            "default": 200000,
            "description": "Maximum result size."
          }
        },
        "required": [
          "jobId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "job_status",
      "description": "Check a VALIDATION job's status (jobs started by validate_with_models/ask_model/etc.) — distinct from llm_job_status, which tracks provider request jobs.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "jobId": {
            "type": "string",
            "minLength": 1,
            "description": "Validation job ID."
          }
        },
        "required": [
          "jobId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "list_available_models",
      "description": "List models and capabilities for every available provider CLI (takes no arguments; complements per-provider list_models).",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "list_models",
      "description": "List models, aliases, and defaults for one provider (claude|codex|gemini|grok|mistral|devin|cursor, or an enabled API provider name), or omit cli to list all providers. API providers are returned under an `apiProviders` array.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "cli": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor"
            ],
            "description": "Provider filter (claude|codex|gemini|grok|mistral|devin|cursor, or an enabled API provider name)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "llm_job_cancel",
      "description": "Cancel a running gateway async or deferred-sync job by jobId.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "jobId": {
            "type": "string",
            "description": "Async job ID from *_request_async"
          }
        },
        "required": [
          "jobId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "llm_job_result",
      "description": "Retrieve captured stdout/stderr for a gateway async or deferred-sync job by jobId. Use rawOutput:true with independent stream offsets for resumable pages.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "jobId": {
            "type": "string",
            "description": "Async job ID from *_request_async"
          },
          "maxChars": {
            "type": "integer",
            "minimum": 1000,
            "maximum": 2000000,
            "default": 200000,
            "description": "Maximum chars returned per stdout/stderr page"
          },
          "stdoutOffsetChars": {
            "type": "integer",
            "minimum": 0,
            "default": 0,
            "description": "Captured stdout character offset for resumable retrieval. Non-zero offsets require rawOutput:true."
          },
          "stderrOffsetChars": {
            "type": "integer",
            "minimum": 0,
            "default": 0,
            "description": "Captured stderr character offset for resumable retrieval. Non-zero offsets require rawOutput:true."
          },
          "rawOutput": {
            "type": "boolean",
            "default": false,
            "description": "Return captured provider streams without display parsing or compression. Local stdio pages concatenate to the captured streams; remote pages redact provider session IDs. Required for resumable offsets."
          }
        },
        "required": [
          "jobId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "llm_job_status",
      "description": "Check lifecycle status and bounded privacy-safe normalized progress for a gateway async or deferred-sync job by jobId.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "jobId": {
            "type": "string",
            "description": "Async job ID from *_request_async"
          },
          "afterProgressSeq": {
            "type": "integer",
            "minimum": 0,
            "default": 0,
            "description": "Return normalized progress events with a sequence greater than this value"
          },
          "progressLimit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 64,
            "default": 32,
            "description": "Maximum next normalized progress events to return in forward sequence order"
          }
        },
        "required": [
          "jobId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "llm_job_watch",
      "description": "Wait briefly for privacy-safe normalized progress on an owned async job. When the MCP request carries a progress token, notifications are emitted only while this watch request remains active.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "jobId": {
            "type": "string",
            "description": "Async job ID from *_request_async"
          },
          "afterProgressSeq": {
            "type": "integer",
            "minimum": 0,
            "default": 0
          },
          "progressLimit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 64,
            "default": 32
          },
          "waitMs": {
            "type": "integer",
            "minimum": 0,
            "maximum": 30000,
            "default": 30000
          }
        },
        "required": [
          "jobId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "llm_process_health",
      "description": "Report gateway process health: async-job manager state plus the resolved persistence configuration and paths.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "llm_request_result",
      "description": "Read back any persisted request (sync or async) from the flight recorder by correlationId, including prompt and response.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "correlationId": {
            "type": "string",
            "minLength": 1,
            "description": "Correlation id from a prior request's structuredContent.correlationId (sync or async)"
          },
          "maxChars": {
            "type": "integer",
            "minimum": 1000,
            "maximum": 2000000,
            "default": 200000,
            "description": "Max chars of the persisted response to return"
          },
          "includePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Include the full persisted prompt text in the result"
          }
        },
        "required": [
          "correlationId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "mistral_request",
      "description": "Run a Mistral Vibe CLI request synchronously (when async jobs are enabled, auto-defers to a pollable job past the sync deadline; otherwise runs to completion). Requires exactly one of prompt or promptParts. Defaults to --agent accept-edits (auto-accepts file edits; dangerous ops such as shell stay gated).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for Mistral Vibe (mutually exclusive with promptParts)"
          },
          "promptParts": {
            "type": "object",
            "properties": {
              "system": {
                "type": "string",
                "maxLength": 100000
              },
              "tools": {
                "type": "string",
                "maxLength": 100000
              },
              "context": {
                "type": "string",
                "maxLength": 100000
              },
              "task": {
                "type": "string",
                "minLength": 1,
                "maxLength": 100000
              },
              "cacheControl": {
                "type": "object",
                "properties": {
                  "system": {
                    "type": "boolean"
                  },
                  "tools": {
                    "type": "boolean"
                  },
                  "context": {
                    "type": "boolean"
                  }
                },
                "additionalProperties": false
              }
            },
            "required": [
              "task"
            ],
            "additionalProperties": false,
            "description": "Cache-aware structured prompt: { system?, tools?, context?, task }. Mutually exclusive with prompt. Stable parts hash into cache_state for prefix-discipline tracking."
          },
          "model": {
            "type": "string",
            "description": "Model alias (e.g. mistral-medium-3.5, latest). Resolved alias is injected via VIBE_ACTIVE_MODEL env var; Vibe has no --model flag."
          },
          "transport": {
            "type": "string",
            "enum": [
              "cli",
              "acp"
            ],
            "default": "cli",
            "description": "Transport: 'cli' (default) runs the Vibe CLI; 'acp' routes through `vibe-acp` when [acp].enabled and the provider's runtime_enabled are set (fails closed otherwise)."
          },
          "outputFormat": {
            "type": "string",
            "enum": [
              "text",
              "plain",
              "json",
              "streaming",
              "stream-json"
            ],
            "description": "Output format for Vibe 2.x (text|json|streaming). Legacy aliases plain→text and stream-json→streaming are accepted."
          },
          "sessionId": {
            "type": "string",
            "description": "Session ID (user-provided CLI handle for --resume). Current Vibe defaults session logging on; doctor flags explicit [session_logging] enabled = false. Note: the gw-* id minted for a brand-new session is not resumable via sessionId; continue with resumeLatest:true."
          },
          "resumeLatest": {
            "type": "boolean",
            "default": false,
            "description": "Resume most recent Vibe session in cwd (--continue). Note: the gw-* id minted for a brand-new session is not resumable via sessionId; continue with resumeLatest:true."
          },
          "createNewSession": {
            "type": "boolean",
            "default": false,
            "description": "Force new session"
          },
          "permissionMode": {
            "type": "string",
            "description": "Vibe --agent name. Legacy requests pass builtins (default|plan|accept-edits|auto-approve), install-gated builtins (e.g. lean), and custom agents from ~/.vibe/agents through to Vibe."
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy: legacy is supported. mcp_managed is rejected before launch because this adapter cannot isolate ambient MCP configuration."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "approvalPolicy is unavailable for this provider. It has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "mcpServers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "description": "This provider does not receive gateway-managed MCP configuration. mcpServers has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "allowedTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Allowlist of built-in tools — each emitted as a separate --enabled-tools <tool> flag"
          },
          "disallowedTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Denylist of built-in tools, each emitted as a separate --disabled-tools <tool> flag"
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "optimizePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Optimize prompt before execution"
          },
          "optimizeResponse": {
            "type": "boolean",
            "default": false,
            "description": "Optimize response output"
          },
          "compressResponse": {
            "type": "boolean",
            "description": "Compress the response display text via the native compressor (default: [compression].enabled in config.toml, off unless opted in). Skipped for structured output."
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h, omit=CLI default)"
          },
          "forceRefresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass dedup and force a fresh CLI run even if a recent identical request exists"
          },
          "trust": {
            "type": "boolean",
            "default": false,
            "description": "Emit `--trust` so Vibe trusts the cwd for this invocation only (not persisted to trusted_folders.toml) and skips the interactive trust prompt (Phase 4 slice γ)."
          },
          "maxTurns": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "minimum": -9007199254740991,
            "maximum": 10000,
            "description": "Vibe `--max-turns N`: cap the agent-loop iteration count (programmatic mode only, Phase 4 slice δ). Bounded to safe integers ≤ 10000."
          },
          "maxPrice": {
            "type": "number",
            "exclusiveMinimum": 0,
            "minimum": 0.000001,
            "maximum": 10000,
            "description": "Vibe `--max-price DOLLARS`: interrupt the session when cumulative cost crosses this cap (programmatic mode only, Phase 4 slice δ). Bounded to finite values ≤ 10000 USD."
          },
          "maxTokens": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "minimum": -9007199254740991,
            "maximum": 100000000,
            "description": "Vibe `--max-tokens N`: cap cumulative prompt + completion tokens for the session (programmatic mode only). Bounded to safe integers ≤ 100000000."
          },
          "workingDir": {
            "type": "string",
            "minLength": 1,
            "description": "Vibe --workdir <DIR>: change to this directory before running. Single value (Vibe accepts one --workdir per invocation). Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "addDir": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Vibe --add-dir <DIR>: additional writable workspace directories. Each entry is emitted as its own --add-dir instance (Vibe states this flag may be specified multiple times). Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Do not use this field, workspace_list, or workspace_register_existing_repo as a fallback for stdio/local provider path access; pass workingDir/addDir/includeDirs directly instead."
          },
          "worktree": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 64
                  },
                  "ref": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 255
                  }
                },
                "additionalProperties": false
              }
            ],
            "description": "Slice λ: run this request inside a dedicated git worktree owned by the gateway. `true` creates a fresh worktree at `<repoRoot>/.worktrees/<uuid>` branched from HEAD. `{ name?, ref? }` lets the caller supply a sanitized name and/or a git ref (default: HEAD). When the request carries a sessionId and the session already has a worktree, reuse requires same-host ownership metadata and a matching live Git registration. Named path collisions never reuse manager state. Gateway-managed worktrees require the local file-backed session manager and fail closed with PostgreSQL sessions. The Grok, Devin, and Mistral adapters require an explicit provider-native sessionId; fresh, createNewSession, and resumeLatest-only worktree requests fail closed because they cannot durably reselect the worktree. The request must select a registered workspace explicitly, through caller-owned session metadata, or through the configured default. Worktrees never fall back to the gateway process cwd and cannot be combined with local workingDir, addDir, or includeDirs paths. The gateway spawns the child CLI with `cwd: <worktree-path>`; no `-w`/`--worktree` flag is ever emitted to the underlying CLI. On worktree materialization, the gateway suppresses repository, system, and global Git hooks and configured clean, smudge, and process checkout filters, sparse checkout, and lazy object fetching. Filter-dependent content such as Git LFS remains in its repository representation instead of executing host commands. On session_delete or TTL eviction the gateway hides the session and runs `git worktree remove --force`. Failed removal retains a durable cleanup-pending tombstone which blocks reuse and is retried when the file store is registered on the owning host. The tombstone is finalized only after verified Git removal. Successful responses are prefixed with `[gateway] worktree=<absolute-path>\\n` so callers can use the path. For Claude approvalStrategy:mcp_managed, requesting or reusing a worktree requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1. Other adapters reject mcp_managed before launch. NOTE: callers should `.gitignore` the `.worktrees/` directory in their repo (the gateway does NOT auto-gitignore; see slice λ spec Q4)."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "mistral_request_async",
      "description": "Start a Mistral Vibe CLI request as a durable background job. Poll with llm_job_status, collect with llm_job_result.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "minLength": 1,
            "maxLength": 100000,
            "description": "Prompt text for Mistral Vibe (mutually exclusive with promptParts)"
          },
          "promptParts": {
            "type": "object",
            "properties": {
              "system": {
                "type": "string",
                "maxLength": 100000
              },
              "tools": {
                "type": "string",
                "maxLength": 100000
              },
              "context": {
                "type": "string",
                "maxLength": 100000
              },
              "task": {
                "type": "string",
                "minLength": 1,
                "maxLength": 100000
              },
              "cacheControl": {
                "type": "object",
                "properties": {
                  "system": {
                    "type": "boolean"
                  },
                  "tools": {
                    "type": "boolean"
                  },
                  "context": {
                    "type": "boolean"
                  }
                },
                "additionalProperties": false
              }
            },
            "required": [
              "task"
            ],
            "additionalProperties": false,
            "description": "Cache-aware structured prompt: { system?, tools?, context?, task }. Mutually exclusive with prompt. Stable parts hash into cache_state for prefix-discipline tracking."
          },
          "model": {
            "type": "string",
            "description": "Model alias (resolved into VIBE_ACTIVE_MODEL env var — Vibe has no --model flag)"
          },
          "outputFormat": {
            "type": "string",
            "enum": [
              "text",
              "plain",
              "json",
              "streaming",
              "stream-json"
            ],
            "description": "Output format for Vibe 2.x (text|json|streaming). Legacy aliases plain→text and stream-json→streaming are accepted."
          },
          "sessionId": {
            "type": "string",
            "description": "Session ID (user-provided CLI handle for --resume). Current Vibe defaults session logging on; doctor flags explicit [session_logging] enabled = false. Note: the gw-* id minted for a brand-new session is not resumable via sessionId; continue with resumeLatest:true."
          },
          "resumeLatest": {
            "type": "boolean",
            "default": false,
            "description": "Resume most recent Vibe session in cwd (--continue). Note: the gw-* id minted for a brand-new session is not resumable via sessionId; continue with resumeLatest:true."
          },
          "createNewSession": {
            "type": "boolean",
            "default": false,
            "description": "Force new session"
          },
          "permissionMode": {
            "type": "string",
            "description": "Vibe --agent name. Legacy requests pass builtins (default|plan|accept-edits|auto-approve), install-gated builtins (e.g. lean), and custom agents from ~/.vibe/agents through to Vibe."
          },
          "approvalStrategy": {
            "type": "string",
            "enum": [
              "legacy",
              "mcp_managed"
            ],
            "default": "legacy",
            "description": "Approval strategy: legacy is supported. mcp_managed is rejected before launch because this adapter cannot isolate ambient MCP configuration."
          },
          "approvalPolicy": {
            "type": "string",
            "enum": [
              "strict",
              "balanced",
              "permissive"
            ],
            "description": "approvalPolicy is unavailable for this provider. It has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "mcpServers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": [],
            "description": "This provider does not receive gateway-managed MCP configuration. mcpServers has no effect with legacy, and mcp_managed is rejected before launch because ambient MCP configuration cannot be isolated."
          },
          "allowedTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Allowlist of built-in tools — each emitted as a separate --enabled-tools <tool> flag"
          },
          "disallowedTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Denylist of built-in tools, each emitted as a separate --disabled-tools <tool> flag"
          },
          "correlationId": {
            "type": "string",
            "description": "Request trace ID (auto if omitted)"
          },
          "optimizePrompt": {
            "type": "boolean",
            "default": false,
            "description": "Optimize prompt before execution"
          },
          "compressResponse": {
            "type": "boolean",
            "description": "Compress the response display text when collected via llm_job_result (native compressor; default: [compression].enabled)."
          },
          "idleTimeoutMs": {
            "type": "integer",
            "minimum": 30000,
            "maximum": 3600000,
            "description": "Idle timeout in ms (min 30s, max 1h, omit=CLI default)"
          },
          "forceRefresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass dedup and force a fresh CLI run even if a recent identical request exists"
          },
          "trust": {
            "type": "boolean",
            "default": false,
            "description": "Emit `--trust` so Vibe trusts the cwd for this invocation only (not persisted to trusted_folders.toml) and skips the interactive trust prompt (Phase 4 slice γ)."
          },
          "maxTurns": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "minimum": -9007199254740991,
            "maximum": 10000,
            "description": "Vibe `--max-turns N`: cap the agent-loop iteration count (programmatic mode only, Phase 4 slice δ). Bounded to safe integers ≤ 10000."
          },
          "maxPrice": {
            "type": "number",
            "exclusiveMinimum": 0,
            "minimum": 0.000001,
            "maximum": 10000,
            "description": "Vibe `--max-price DOLLARS`: interrupt the session when cumulative cost crosses this cap (programmatic mode only, Phase 4 slice δ). Bounded to finite values ≤ 10000 USD."
          },
          "maxTokens": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "minimum": -9007199254740991,
            "maximum": 100000000,
            "description": "Vibe `--max-tokens N`: cap cumulative prompt + completion tokens for the session (programmatic mode only). Bounded to safe integers ≤ 100000000."
          },
          "workingDir": {
            "type": "string",
            "minLength": 1,
            "description": "Vibe --workdir <DIR>: change to this directory before running. Single value per invocation. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "addDir": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Vibe --add-dir <DIR>: additional writable workspace directories. Each entry is emitted as its own --add-dir instance. Stdio/local callers may pass local paths directly. Remote HTTP/OAuth callers must use relative paths inside a selected registered workspace. Do not call workspace_* tools to fix stdio/local provider path access."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Do not use this field, workspace_list, or workspace_register_existing_repo as a fallback for stdio/local provider path access; pass workingDir/addDir/includeDirs directly instead."
          },
          "worktree": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "object",
                "properties": {
                  "name": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 64
                  },
                  "ref": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 255
                  }
                },
                "additionalProperties": false
              }
            ],
            "description": "Slice λ: run this request inside a dedicated git worktree owned by the gateway. `true` creates a fresh worktree at `<repoRoot>/.worktrees/<uuid>` branched from HEAD. `{ name?, ref? }` lets the caller supply a sanitized name and/or a git ref (default: HEAD). When the request carries a sessionId and the session already has a worktree, reuse requires same-host ownership metadata and a matching live Git registration. Named path collisions never reuse manager state. Gateway-managed worktrees require the local file-backed session manager and fail closed with PostgreSQL sessions. The Grok, Devin, and Mistral adapters require an explicit provider-native sessionId; fresh, createNewSession, and resumeLatest-only worktree requests fail closed because they cannot durably reselect the worktree. The request must select a registered workspace explicitly, through caller-owned session metadata, or through the configured default. Worktrees never fall back to the gateway process cwd and cannot be combined with local workingDir, addDir, or includeDirs paths. The gateway spawns the child CLI with `cwd: <worktree-path>`; no `-w`/`--worktree` flag is ever emitted to the underlying CLI. On worktree materialization, the gateway suppresses repository, system, and global Git hooks and configured clean, smudge, and process checkout filters, sparse checkout, and lazy object fetching. Filter-dependent content such as Git LFS remains in its repository representation instead of executing host commands. On session_delete or TTL eviction the gateway hides the session and runs `git worktree remove --force`. Failed removal retains a durable cleanup-pending tombstone which blocks reuse and is retried when the file store is registered on the owning host. The tombstone is finalized only after verified Git removal. Successful responses are prefixed with `[gateway] worktree=<absolute-path>\\n` so callers can use the path. For Claude approvalStrategy:mcp_managed, requesting or reusing a worktree requires approval and LLM_GATEWAY_APPROVAL_ALLOW_BYPASS=1. Other adapters reject mcp_managed before launch. NOTE: callers should `.gitignore` the `.worktrees/` directory in their repo (the gateway does NOT auto-gitignore; see slice λ spec Q4)."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "provider_admin_list",
      "description": "List provider CLI admin operations (auth status, model list, mcp list, plugin list, doctor, etc.) available on the installed CLIs, projected from runtime discovery. Read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "provider": {
            "description": "Optional provider filter",
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor"
            ]
          },
          "includeUnavailable": {
            "default": false,
            "description": "Include operations the installed CLI does not advertise or the policy hides",
            "type": "boolean"
          }
        },
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "provider_admin_mutate",
      "description": "Execute a MUTATING provider CLI admin operation (mcp add/remove, login/logout, plugin install/remove, session delete/archive, ...). Disabled unless [admin] allow_mutating_cli_admin_ops=true; routed through approval and audited.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "provider": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor"
            ],
            "description": "Provider whose admin operation to run"
          },
          "operationId": {
            "type": "string",
            "minLength": 1,
            "description": "Mutating operation id from provider_admin_list, e.g. 'mcp.remove'"
          }
        },
        "required": [
          "provider",
          "operationId"
        ],
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "provider_admin_run",
      "description": "Execute a READ-ONLY provider CLI admin operation (from provider_admin_list) and return redacted output. Rejects mutating operations.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "provider": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor"
            ],
            "description": "Provider whose admin operation to run"
          },
          "operationId": {
            "type": "string",
            "minLength": 1,
            "description": "Operation id from provider_admin_list, e.g. 'mcp.list' or 'doctor'"
          }
        },
        "required": [
          "provider",
          "operationId"
        ],
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "provider_subcommand_contract",
      "description": "Return the detailed read-only contract for exactly one declared provider CLI subcommand.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "provider": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor"
            ],
            "description": "Provider (claude|codex|gemini|grok|mistral|devin|cursor)"
          },
          "commandPath": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            },
            "minItems": 1,
            "description": "Command path segments"
          }
        },
        "required": [
          "provider",
          "commandPath"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "provider_subcommand_drift",
      "description": "Probe declared provider subcommand --help surfaces and return compact drift rows without raw help output.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "provider": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor"
            ],
            "description": "Optional provider filter (claude|codex|gemini|grok|mistral|devin|cursor)"
          },
          "includeClean": {
            "type": "boolean",
            "default": false,
            "description": "When false, return only unavailable or drifted command paths"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "provider_subcommands_list",
      "description": "Return a compact, filterable read-only catalog of declared provider CLI subcommands without flags or raw help.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "provider": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor"
            ],
            "description": "Optional provider filter (claude|codex|gemini|grok|mistral|devin|cursor)"
          },
          "tier": {
            "type": "string",
            "enum": [
              "catalog",
              "inspect",
              "execute_candidate",
              "diagnostic"
            ],
            "description": "Optional subcommand tier filter"
          },
          "risk": {
            "type": "string",
            "enum": [
              "read_only",
              "writes_local_config",
              "auth",
              "network",
              "starts_server",
              "updates_binary",
              "destructive",
              "executes_agent"
            ],
            "description": "Optional risk classification filter"
          },
          "exposure": {
            "type": "string",
            "enum": [
              "tracked_only",
              "mcp_readonly",
              "mcp_requires_approval",
              "not_exposed"
            ],
            "description": "Optional MCP exposure filter"
          },
          "commandPathPrefix": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1
            },
            "description": "Optional command path prefix filter, e.g. ['agent']"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "provider_tool_capabilities",
      "description": "Report provider tool/feature capabilities and discovered local skill/tool integrations for claude|codex|gemini|grok|mistral|devin|cursor|grok_api, or an enabled API provider name.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "cli": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor",
              "grok_api"
            ],
            "description": "Provider filter (claude|codex|gemini|grok|mistral|devin|cursor|grok_api, or an enabled API provider name)"
          },
          "includeSkills": {
            "type": "boolean",
            "default": true,
            "description": "Include bounded local skill discovery results"
          },
          "includeProviderTools": {
            "type": "boolean",
            "default": true,
            "description": "Include provider-native tools extracted from local skills"
          },
          "includeUnsupported": {
            "type": "boolean",
            "default": true,
            "description": "Include explicit unsupported/degraded input records"
          },
          "includePaths": {
            "type": "boolean",
            "default": false,
            "description": "Include raw local filesystem paths in discovery output"
          },
          "refresh": {
            "type": "boolean",
            "default": false,
            "description": "Bypass the short-lived capability cache"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "red_team_review",
      "description": "Challenge a plan, answer, or document for risks and failure modes via provider CLIs (starts validation jobs).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "content": {
            "type": "string",
            "minLength": 1,
            "description": "Plan, answer, or document to challenge."
          },
          "riskLevel": {
            "type": "string",
            "enum": [
              "normal",
              "high"
            ],
            "default": "normal",
            "description": "How aggressively to review."
          },
          "models": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "claude",
                "codex",
                "gemini",
                "grok",
                "mistral",
                "devin",
                "cursor"
              ]
            },
            "minItems": 1,
            "default": [
              "claude",
              "codex"
            ],
            "description": "Providers to ask for adversarial review."
          },
          "select": {
            "type": "string",
            "enum": [
              "cheapest",
              "cheapest_per_tier"
            ],
            "description": "Optional least-cost routing: fill the provider target(s) from the LCR selector instead of the explicit list. 'cheapest' picks the single cheapest eligible provider; 'cheapest_per_tier' picks the cheapest in each quality tier. Requires [least_cost].enabled=true; fails closed (no default-list fallback) when disabled or nothing is eligible."
          }
        },
        "required": [
          "content"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "review_changes",
      "description": "Capture one complete, immutable Git evidence artifact, fence it as untrusted data, and start independent read-only provider reviews. Includes committed, staged, unstaged, and untracked changes without truncation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "workingDir": {
            "type": "string",
            "minLength": 1,
            "description": "Absolute local path to the checkout. Stdio/local callers should use this; remote HTTP/OAuth callers must use workspace instead."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Authorized workspace alias, required for remote HTTP/OAuth callers."
          },
          "scope": {
            "type": "string",
            "enum": [
              "auto",
              "uncommitted",
              "branch",
              "commit"
            ],
            "default": "auto",
            "description": "Review scope. auto reviews a diverged branch from its merge-base with working-tree evidence included, otherwise reviews dirty uncommitted changes, and falls back to the last commit when the tree is clean."
          },
          "base": {
            "type": "string",
            "minLength": 1,
            "maxLength": 512,
            "description": "Explicit Git base ref or commit. Overrides automatic base selection."
          },
          "paths": {
            "type": "array",
            "items": {
              "type": "string",
              "minLength": 1,
              "maxLength": 4096
            },
            "maxItems": 256,
            "description": "Optional literal repository-relative path filters."
          },
          "stance": {
            "type": "string",
            "enum": [
              "standard",
              "adversarial"
            ],
            "default": "standard"
          },
          "focus": {
            "type": "string",
            "maxLength": 20000,
            "description": "Additional reviewer focus outside the untrusted evidence boundary."
          },
          "models": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "claude",
                "codex",
                "gemini",
                "grok",
                "mistral",
                "devin",
                "cursor"
              ]
            },
            "minItems": 1,
            "default": [
              "claude",
              "codex"
            ],
            "description": "Independent providers to start. Defaults to Claude and Codex."
          },
          "judgeModel": {
            "$ref": "#/properties/models/items",
            "description": "Optional judge provider to reconcile terminal reviews in a second step. An HTTP/API judge requires allowApiUpload=true, which is bound to the durable validationId."
          },
          "allowApiUpload": {
            "type": "boolean",
            "default": false,
            "description": "Explicitly allow repository review evidence to be sent to configured HTTP/API reviewers or a planned API judge. API judge consent is bound to the durable validationId; remote workspace reviews do not permit API upload."
          },
          "maxArtifactBytes": {
            "type": "integer",
            "minimum": 1024,
            "maximum": 16777216,
            "default": 120000,
            "description": "Fail-closed byte ceiling for the complete serialized Git artifact."
          },
          "maxPromptBytes": {
            "type": "integer",
            "minimum": 1024,
            "maximum": 16777216,
            "default": 128000,
            "description": "Fail-closed byte ceiling for the fenced provider prompt."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "second_opinion",
      "description": "Ask one provider CLI to review an answer (starts a validation job; poll job_status, collect job_result).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "answer": {
            "type": "string",
            "minLength": 1,
            "description": "Answer to review."
          },
          "question": {
            "type": "string",
            "description": "Original question, if available."
          },
          "model": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor"
            ],
            "default": "codex",
            "description": "Provider to ask for the second opinion."
          },
          "select": {
            "type": "string",
            "enum": [
              "cheapest",
              "cheapest_per_tier"
            ],
            "description": "Optional least-cost routing: fill the provider target(s) from the LCR selector instead of the explicit list. 'cheapest' picks the single cheapest eligible provider; 'cheapest_per_tier' picks the cheapest in each quality tier. Requires [least_cost].enabled=true; fails closed (no default-list fallback) when disabled or nothing is eligible."
          }
        },
        "required": [
          "answer"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "session_clear_all",
      "description": "Delete all gateway session records, optionally scoped to one provider.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "cli": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor",
              "grok-api"
            ],
            "description": "Provider filter (claude|codex|gemini|grok|mistral|grok-api)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "session_create",
      "description": "Create a gateway session record for a provider. NOTE: this is gateway bookkeeping (a plain UUID), not a provider-native session; Codex resume needs a real Codex UUID.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "cli": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor",
              "grok-api"
            ],
            "description": "Provider type (claude|codex|gemini|grok|mistral|grok-api)"
          },
          "description": {
            "type": "string",
            "description": "Session description"
          },
          "setAsActive": {
            "type": "boolean",
            "default": true,
            "description": "Set as active session"
          }
        },
        "required": [
          "cli"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "session_delete",
      "description": "Delete a gateway session record by ID (also removes any gateway-owned worktree attached to it).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sessionId": {
            "type": "string",
            "description": "Session ID"
          }
        },
        "required": [
          "sessionId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "session_get",
      "description": "Get one gateway session record by session ID, including recent request history when available.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sessionId": {
            "type": "string",
            "description": "Session ID"
          }
        },
        "required": [
          "sessionId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "session_list",
      "description": "List gateway session records and the active session per provider, optionally filtered by provider.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "cli": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor",
              "grok-api"
            ],
            "description": "Provider filter (claude|codex|gemini|grok|mistral|grok-api)"
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "session_set_active",
      "description": "Set or clear the active session for a provider; the active session is used when a request omits sessionId.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "cli": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor",
              "grok-api"
            ],
            "description": "Provider type (claude|codex|gemini|grok|mistral|grok-api)"
          },
          "sessionId": {
            "type": [
              "string",
              "null"
            ],
            "description": "Session ID (null to clear)"
          }
        },
        "required": [
          "cli",
          "sessionId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "synthesize_validation",
      "description": "Run an explicit judge model over validation results. General validation uses caller-supplied terminal results; review_changes rebuilds its question and results from the owned durable run.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "question": {
            "type": "string",
            "minLength": 1,
            "description": "Original request for general validation. Ignored for review_changes, which uses the question stored in the durable run."
          },
          "providerResults": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "provider": {
                  "type": "string",
                  "enum": [
                    "claude",
                    "codex",
                    "gemini",
                    "grok",
                    "mistral",
                    "devin",
                    "cursor"
                  ]
                },
                "model": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "status": {
                  "type": "string",
                  "enum": [
                    "running",
                    "completed",
                    "failed",
                    "canceled",
                    "orphaned",
                    "skipped"
                  ]
                },
                "verdict": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "rationale": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "risks": {
                  "type": "array",
                  "items": {
                    "type": "string"
                  },
                  "default": []
                },
                "rawJobReference": {
                  "anyOf": [
                    {
                      "type": "object",
                      "properties": {
                        "jobId": {
                          "type": "string"
                        },
                        "correlationId": {
                          "type": "string"
                        },
                        "statusTool": {
                          "type": "string",
                          "const": "job_status"
                        },
                        "resultTool": {
                          "type": "string",
                          "const": "job_result"
                        }
                      },
                      "required": [
                        "jobId",
                        "correlationId",
                        "statusTool",
                        "resultTool"
                      ],
                      "additionalProperties": false
                    },
                    {
                      "type": "null"
                    }
                  ]
                },
                "error": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "warning": {
                  "type": "string"
                }
              },
              "required": [
                "provider",
                "model",
                "status",
                "verdict",
                "rationale",
                "rawJobReference",
                "error"
              ],
              "additionalProperties": false
            },
            "default": [],
            "description": "Terminal normalized results for general validation. Ignored for review_changes, which reloads every exact linked durable job result."
          },
          "judgeModel": {
            "$ref": "#/properties/providerResults/items/properties/provider",
            "default": "codex",
            "description": "Provider to run the judge synthesis."
          },
          "validationId": {
            "type": "string",
            "description": "Run id from kickoff. Optional for general validation, but required for review_changes so the stored question, provider jobs, repository, planned judge, and upload policy can be enforced."
          },
          "workingDir": {
            "type": "string",
            "minLength": 1,
            "description": "For a review_changes run, the same absolute local checkout path used at kickoff."
          },
          "workspace": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "For a remote review_changes run, the same authorized workspace alias."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "upstream_contracts",
      "description": "Return the gateway's declared provider CLI contracts; with probeInstalled true, diff against installed --help surfaces to detect flag drift.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "cli": {
            "type": "string",
            "enum": [
              "claude",
              "codex",
              "gemini",
              "grok",
              "mistral",
              "devin",
              "cursor"
            ],
            "description": "CLI filter (claude|codex|gemini|grok|mistral|devin|cursor)"
          },
          "probeInstalled": {
            "type": "boolean",
            "default": false,
            "description": "When true, run local --help probes and compare advertised flags against the declared contract. Strongly recommended after any provider CLI upgrade to detect drift."
          }
        },
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "validate_with_models",
      "description": "Ask two or more provider CLIs to independently validate a question. Starts validation jobs — poll with job_status, collect with job_result (not llm_job_*).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "question": {
            "type": "string",
            "minLength": 1,
            "description": "Question or content to validate."
          },
          "models": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "claude",
                "codex",
                "gemini",
                "grok",
                "mistral",
                "devin",
                "cursor"
              ]
            },
            "minItems": 1,
            "default": [
              "claude",
              "codex"
            ],
            "description": "Providers to ask. Defaults to Claude and Codex."
          },
          "focus": {
            "type": "string",
            "default": "correctness, missing assumptions, and practical next steps",
            "description": "What reviewers should pay attention to."
          },
          "judgeModel": {
            "$ref": "#/properties/models/items",
            "description": "Optional provider to run an explicit judge synthesis job."
          },
          "select": {
            "type": "string",
            "enum": [
              "cheapest",
              "cheapest_per_tier"
            ],
            "description": "Optional least-cost routing: fill the provider target(s) from the LCR selector instead of the explicit list. 'cheapest' picks the single cheapest eligible provider; 'cheapest_per_tier' picks the cheapest in each quality tier. Requires [least_cost].enabled=true; fails closed (no default-list fallback) when disabled or nothing is eligible."
          }
        },
        "required": [
          "question"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "validation_receipt",
      "description": "Retrieve the canonically hashed immutable receipt of a terminal cross-LLM validation run by validationId. Returns minted | pending | expired_unminted (no receipt exists and none can be minted) | verification_failed (a stored receipt does not verify against its run) | not_found (own-or-not-found).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "validationId": {
            "type": "string",
            "minLength": 1,
            "description": "The run-level validationId from a validation kickoff response (not a job/correlation id)."
          },
          "format": {
            "type": "string",
            "enum": [
              "json",
              "markdown"
            ],
            "default": "json",
            "description": "Response format. markdown returns the human-readable rendering (derived on read, never stored or hashed)."
          },
          "includeRawResponses": {
            "type": "boolean",
            "default": false,
            "description": "Inline complete provider answer text when the owned linked job still exposes identity-verified output (read-time only; never persisted or hashed)."
          }
        },
        "required": [
          "validationId"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "workspace_create",
      "description": "Create a remote HTTP/OAuth workspace alias by creating a new local folder or git repo under a configured allowed root. Not for stdio/local provider path access. Requires LLM_GATEWAY_WORKSPACE_ADMIN=1 and OAuth scope workspace:admin.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "alias": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Stdio/local callers should not use workspace_* tools to fix provider path access; pass local workingDir/addDir/includeDirs directly."
          },
          "root": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Allowed-root alias from workspace_list."
          },
          "slug": {
            "type": "string",
            "minLength": 1,
            "maxLength": 255,
            "description": "Safe relative path under the allowed root."
          },
          "kind": {
            "type": "string",
            "enum": [
              "folder",
              "git"
            ],
            "default": "git"
          },
          "setDefault": {
            "type": "boolean",
            "default": false
          }
        },
        "required": [
          "alias",
          "root",
          "slug"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "workspace_get",
      "description": "Inspect a registered remote HTTP/OAuth workspace alias. Does not list files. Not needed for stdio/local provider calls; do not use workspace_* tools to fix local path access.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "alias": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Stdio/local callers should not use workspace_* tools to fix provider path access; pass local workingDir/addDir/includeDirs directly."
          }
        },
        "required": [
          "alias"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "workspace_list",
      "description": "List registered workspace aliases for remote HTTP/OAuth provider calls. Does not browse files. Stdio/local callers should not use workspace_* tools to fix provider path access; pass local workingDir/addDir/includeDirs directly.",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    },
    {
      "name": "workspace_register_existing_repo",
      "description": "Register an existing local Git repo as a remote HTTP/OAuth workspace alias. Not for stdio/local provider path access. Requires LLM_GATEWAY_WORKSPACE_ADMIN=1 and OAuth scope workspace:admin.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "alias": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64,
            "pattern": "^[A-Za-z][A-Za-z0-9._-]{0,63}$",
            "description": "Registered workspace alias for remote HTTP/OAuth provider calls. Stdio/local callers should not use workspace_* tools to fix provider path access; pass local workingDir/addDir/includeDirs directly."
          },
          "path": {
            "type": "string",
            "minLength": 1,
            "description": "Absolute path to an existing Git repo under an allowed root."
          },
          "setDefault": {
            "type": "boolean",
            "default": false
          }
        },
        "required": [
          "alias",
          "path"
        ],
        "additionalProperties": false,
        "$schema": "http://json-schema.org/draft-07/schema#"
      }
    }
  ]
}
